export const meta = {
  name: 'kit-tareas-hermanos-5',
  description: 'Tanda 5 (cierre) de los hermanos «▸»: motor 2.4 (FRASE_NIVELES condicionada, dedupe sin fila, digest con DV) + SPEC §9 → mini-ronda de contenido (dark-kitchen, heladería, catering, chocolatería, restaurante-creativo) → verificación focalizada de los 11 + diff firmado del representante → crítico',
  phases: [
    { title: 'Motor 2.4', detail: 'opus: 3 fixes + SPEC §9 + regresión', model: 'opus' },
    { title: 'Contenido', detail: 'mini-ronda: dark-kitchen, catering, chocolatería, restaurante-creativo (opus) · heladería (sonnet)', model: 'opus' },
    { title: 'Verificar', detail: 'sonnet por kit (2 a la vez), focalizada; JSON al repo', model: 'sonnet' },
    { title: 'Crítico', detail: 'opus: decide listos/bloqueados', model: 'opus' },
  ],
}
const PAR = args && args.par ? args.par : 2
const REPO = '/Users/johnguerrero/chefpro-modernize'
const SCR = '/private/tmp/claude-501/-Users-johnguerrero-chefpro-modernize/7340312f-b4fe-4aa1-b254-4b0c17c8375f/scratchpad'
const SPEC = `${REPO}/scripts/productos-digitales/kit-tareas-v2-SPEC.md`
const PKG = `${REPO}/scripts/productos-digitales/kit-tareas-v2_0`
const DLROOT = `${REPO}/astro-site/public/dl`
const VER = `${REPO}/scripts/productos-digitales/auditorias/kit-tareas-hermanos`
const OUT = `${SCR}/kt-hermanos-5`
const HERMANOS = ['kit-tareas-cafeteria', 'kit-tareas-pizzeria', 'kit-tareas-hamburgueseria', 'kit-tareas-dark-kitchen', 'kit-tareas-bar', 'kit-tareas-catering', 'kit-tareas-chocolateria', 'kit-tareas-heladeria', 'kit-tareas-hotel', 'kit-tareas-restaurante-creativo']
const TODOS = HERMANOS.concat(['kit-tareas'])
const RULES = `REGLAS DURAS: (1) PROHIBIDO modificar ${DLROOT}/** — usa \`CLAUDE_SCRATCHPAD=${SCR}/propio-<tu-rol> python3 ${PKG}/main.py --producto <pid> --dry-run --json ${OUT}/<pid>.json\` (nunca KIT_TAREAS_APPLY). (2) REGLA TÉRMICA ESTRICTA (el Mac se apaga a 65 °C): \`istats cpu temp\` ANTES de cada python; si ≥ 60 °C → \`sleep 90\` y vuelve a medir; nunca dos python a la vez; nada de builds/Playwright. (3) No git add/commit/stash/clean. (4) Cita fichero:hoja:celda. (5) JSON según schema. (6) El representante kit-tareas (en producción) sólo en --dry-run. (7) Escribe tu salida (código o JSON) PRONTO en el REPO y ve actualizándola. (8) python3 es 3.7 / openpyxl 3.1.3: sin f-strings con '=' ni walrus. (9) Los .xlsx llevan U+202F y U+2011: en código, por escape.`
const FIRMAS = `FIRMAS DEL ORQUESTADOR (tanda 5; base ${VER}/critico-3.json y ${VER}/motor-2.3.json): (d) FIRMADO: el paréntesis del fichero de negocio se deriva de sus bandas reales y, si no se puede, se OMITE (nunca «terraza» inventada); en el representante queda sin paréntesis. (e) FIRMADO: la cola «Estás en …» del fichero de CAJA es la específica de caja. (c) aceptado: alturas autoalto donde el texto creció. (f) aceptado: el calendario del representante desbloquea A5:D27 (23 filas; la 27 vacía). BAR 07 «Zona» vacía en verde: aceptado. T-06: sin acción. UMBRAL_BANDA 0,8: se mantiene. CRITERIO «caja fuera del 09» (hamburguesería 01/04, pizzería 01, restaurante-creativo 01, heladería 01!B36): NO bloquea esta tanda, se documenta en SPEC §9 como pendiente. MOTOR 2.4: (m1) FRASE_NIVELES condicionada a CTX['f_areas']: en los kits sin fichero de áreas la viñeta dice «Cada fichero cubre un nivel: el de negocio marca el HITO (encender, abrir, cerrar) y el de caja lleva el DINERO. Si una tarea aparece en los dos, es a propósito: una es el hito y la otra el detalle.» (10 celdas: catering 08!B38/09!B49, chocolatería 08!B37/09!B48, heladería 08!B37/09!B48, hotel 18!B37/19!B48, restaurante-creativo 10!B37/11!B48). (m2) gates.bandas_solapadas deduplicada por (hoja SIN nº de fila, banda, tareas, casadas, ratio). (m3) main.digest compara también error/errorTitle/prompt de cada DataValidation y la protección (locked) de celdas, para que el diff de idempotencia y el diff contra producción sean completos. (m4) SPEC §9: reglas de las tandas 3-5 (i)-(l), T-01..T-08, (d)-(f), m1-m3 y el pendiente «caja fuera del 09», con la misma forma que §8.`
const BUILD = { type: 'object', properties: { ficheros_codigo: { type: 'array', items: { type: 'string' } }, pruebas: { type: 'array', items: { type: 'string' } }, afecta_al_representante: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } }, notas: { type: 'array', items: { type: 'string' } } }, required: ['ficheros_codigo', 'pruebas', 'afecta_al_representante', 'pendiente', 'notas'] }

phase('Motor 2.4')
const m = await agent(`Mantenedor de ${PKG}/ (lee motor.py y main.py ENTEROS) y de ${SPEC}. Lee ${VER}/critico-3.json y ${VER}/motor-2.3.json. ${FIRMAS}\n\nAplica m1, m2, m3 y escribe m4 (SPEC §9). Prueba en dry-run, EN SERIE, con istats antes de cada uno: kit-tareas-catering (m1: 08!B38 y 09!B49 con la frase de 2 niveles; idempotencia 0), kit-tareas-dark-kitchen (m2: bandas_solapadas sin la banda «PREPARACIÓN MAÑANA» repetida), kit-tareas-cafeteria (regresión, frase de 3 niveles intacta), kit-tareas (representante: con el digest m3, lista el diff COMPLETO contra ${DLROOT}/kit-tareas por categorías con recuento — valores, mensajes de DV, alturas, locked — y escríbelo en ${VER}/kit-tareas-diff-firmado.json; se espera ≈177: 26 valor + 33 DV + 26-28 alturas + 92 locked; cualquier categoría nueva va a pendiente). Informe en ${VER}/motor-2.4.json. ${RULES}`,
  { label: 'motor-2.4', phase: 'Motor 2.4', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor 2.4: ${m ? `pendiente ${m.pendiente.length}` : 'NULL'}`)

phase('Contenido')
const CONT = { type: 'object', properties: { pid: { type: 'string' }, fichero_codigo: { type: 'string' }, aplicados: { type: 'array', items: { type: 'string' } }, no_aplicados: { type: 'array', items: { type: 'object', properties: { tema: { type: 'string' }, motivo: { type: 'string' } }, required: ['tema', 'motivo'] } }, dryrun_exit: { type: 'integer' }, notas: { type: 'array', items: { type: 'string' } } }, required: ['pid', 'fichero_codigo', 'aplicados', 'no_aplicados', 'dryrun_exit', 'notas'] }
const FIXES = [
  { pid: 'kit-tareas-dark-kitchen', model: 'opus', tarea: `BLOQUEO de la tanda 4: _f08 reescribe 7 de 12 tareas del 08 y deja vivas, en un negocio 100 % delivery sin público: 'Apertura del Negocio'!B9 «Montar señalización exterior / pizarra de menú», !B12 «Poner música ambiente al volumen adecuado», !B22 «Activar restaurante en plataformas»; 'Cierre del Negocio'!B5 «Apagar música ambiente», !B18 «Desactivar restaurante en plataformas delivery». Sustituciones 1:1 (misma fila, D/E intactas) al vocabulario del kit («marca(s)», no «restaurante»): p.ej. B9 → «Comprobar señalización del punto de recogida de riders y cartel de horario», B12 → «Comprobar que tablets/impresoras de cada marca tienen papel y batería», B22 → «Activar las marcas en las plataformas (Glovo, Uber Eats, Just Eat) y comprobar tiempos publicados», B5 → «Pausar pedidos nuevos en todas las marcas (cierre de cocina)», B18 → «Desactivar las marcas en las plataformas y anotar pedidos rechazados/cancelados». Después barre las 12 tareas del 08 y las Instrucciones de los 11 ficheros buscando «restaurante», «sala», «terraza», «reservas», «comensal» y resuélvelo o justifica cada resto.` },
  { pid: 'kit-tareas-heladeria', model: 'sonnet', tarea: `(1) 01-apertura-cierre.xlsx:'Apertura'!B16 y 08-apertura-cierre-negocio.xlsx:'Apertura del Negocio'!B20-B21 piden anotar la MISMA lectura de temperatura de la misma vitrina con dos huecos «____ °C»: deja el hueco SOLO en el 01 (detalle) y reescribe B20-B21 del 08 como hito sin hueco («Vitrinas de helado en temperatura (−12/−14 °C; lectura anotada en 01-apertura-cierre)» y el equivalente para la segunda). (2) 08:'Apertura del Negocio'!C11 zona «Recepción» → «Mostrador». Sustituciones 1:1 vía el módulo; demuestra con celda; idempotencia 0.` },
  { pid: 'kit-tareas-catering', model: 'opus', tarea: `Vocabulario de restaurante con sala en el 08 de una empresa de producción off-site: 08-apertura-cierre-negocio.xlsx:'Apertura del Negocio'!B5/B9/B11/B12/B14/B18/B19 (pizarra de menú, reservas, música ambiente, cartas/menús…). Reescribe 1:1 (misma fila, D/E intactas) al oficio del catering: órdenes de servicio del día, hoja de ruta de vehículos, carga de isotermos y registro de temperatura de salida, check de material por evento, confirmación de accesos del recinto, briefing del equipo de sala del evento… Después barre las Instrucciones y el 08/09 enteros buscando «carta», «pizarra», «reservas», «música», «comensales en sala» y resuelve o justifica.` },
  { pid: 'kit-tareas-chocolateria', model: 'opus', tarea: `08-apertura-cierre-negocio.xlsx:'Apertura del Negocio'!B9/B11/B13/B18: carta y pizarra de menú en una bombonería de mostrador. Reescribe 1:1 al oficio (vitrina de bombones y tabletas: reposición, etiquetas de precio y alérgenos, temperatura de vitrina 16-18 °C y humedad < 55 %, cajas y packaging de regalo, pedidos por encargo del día). Barre después el 08/09 y las Instrucciones buscando «carta», «pizarra», «menú», «mesa», «comensal» y resuelve o justifica.` },
  { pid: 'kit-tareas-restaurante-creativo', model: 'opus', tarea: `DOM-07 residual: 09-plantilla-personalizable.xlsx trae 3 filas de ejemplo por hoja (marcadas N/A, bien) pero Plantilla A / B / C son IDÉNTICAS; la SPEC §2.3 pide diferenciar por franja / área-partida / perfil. Desde el módulo: renombra los rótulos de sección de cada hoja y sus 3 ejemplos para que A = por FRANJA (mise en place mañana / pase / cierre), B = por PARTIDA (I+D, frío, caliente, pastelería) y C = por PERFIL (jefe de cocina, jefe de partida, sumiller, jefe de sala), sin mover el contador ni la maquetación; demuestra con celdas; idempotencia 0.` },
]
const contPrompt = (f) => `Mantenedor del módulo de CONTENIDO ${PKG}/contenido_${f.pid.replace(/-/g, '_')}.py del kit «${f.pid}» (léelo ENTERO; motor ${PKG}/motor.py ya en 2.4). SPEC: ${SPEC} (incluido §9). Verificación previa: ${VER}/${f.pid}-ver3.json. TAREA: ${f.tarea}\n\nRegla: texto 1:1 en celdas existentes; nunca maquetación; todo idempotente. Prueba: \`CLAUDE_SCRATCHPAD=${SCR}/propio-cont-${f.pid} python3 ${PKG}/main.py --producto ${f.pid} --dry-run --json ${OUT}/${f.pid}-cont.json\` → exit 0, idempotencia 0, gate negocio_precargado verde; demuestra cada cambio abriendo la celda. ${RULES}`
const conts = []
for (let i = 0; i < FIXES.length; i += PAR) {
  const par = FIXES.slice(i, i + PAR)
  const r = await parallel(par.map((f) => () => agent(contPrompt(f), { label: `contenido:${f.pid}`, phase: 'Contenido', model: f.model, effort: 'high', schema: CONT })))
  conts.push(...r)
  log(`contenido ${par.map((f) => f.pid).join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit} aplicados ${x.aplicados.length} no_aplicados ${x.no_aplicados.length}` : 'NULL')).join(' · ')}`)
}

phase('Verificar')
const VS = { type: 'object', properties: { pid: { type: 'string' }, dryrun_exit: { type: 'integer' }, censo_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, listas_dv: { type: 'array', items: { type: 'string' } }, casos6_ok: { type: 'integer' }, casos6_total: { type: 'integer' }, frase_niveles_ok: { type: 'boolean' }, recuento_gate: { type: 'integer' }, fechas_bonus02: { type: 'integer' }, tareas_01: { type: 'integer' }, hojas_checklist: { type: 'integer' }, ficheros_total: { type: 'integer' }, fixes_tanda5_ok: { type: 'boolean' }, hallazgos_nuevos: { type: 'array', items: { type: 'object', properties: { severidad: { type: 'string', enum: ['alta', 'media', 'baja'] }, donde: { type: 'string' }, hallazgo: { type: 'string' } }, required: ['severidad', 'donde', 'hallazgo'] } }, bloquea_real: { type: 'boolean' }, fichero_json: { type: 'string' } }, required: ['pid', 'dryrun_exit', 'censo_exit', 'idempotencia', 'listas_dv', 'casos6_ok', 'casos6_total', 'frase_niveles_ok', 'recuento_gate', 'fechas_bonus02', 'tareas_01', 'hojas_checklist', 'ficheros_total', 'fixes_tanda5_ok', 'hallazgos_nuevos', 'bloquea_real', 'fichero_json'] }
const verPrompt = (pid) => {
  const esRep = pid === 'kit-tareas'
  const extra = esRep
    ? `REPRESENTANTE EN PRODUCCIÓN: regenera en dry-run y haz TU diff celda a celda (valores, fórmulas, DV incl. mensajes, alturas, locked, merges, print_area) contra ${DLROOT}/kit-tareas; compáralo con ${VER}/kit-tareas-diff-firmado.json del motor 2.4: fixes_tanda5_ok = true sólo si tu diff y el firmado coinciden categoría a categoría (±2) y no hay NINGUNA diferencia fuera de las firmadas (DV msg, 09!B10, 08 Instrucciones T-01/T-08, 09 TPV, 09 cola de caja, paréntesis omitido, frase T-08, alturas, calendario A5:D27 locked). Cualquier otra diferencia = hallazgo ALTA + bloquea_real.`
    : `Verificación previa: ${VER}/${pid}-ver3.json; fixes de esta tanda (si los hay) en ${PKG}/contenido_${pid.replace(/-/g, '_')}.py y ${VER}/motor-2.4.json. fixes_tanda5_ok = true si cada fix declarado para tu kit está en la celda y el de motor m1 se cumple.`
  return `Verificador independiente del kit «${pid}» (tanda 5, focalizada). SPEC: ${SPEC} (§9). ${extra} Regenera en dry-run y comprueba: (1) exit, censo --fail, 2.ª pasada (diferencias con el digest nuevo m3), UNA lista de DV, casos §6 con pycel; (2) frase_niveles_ok: en las Instrucciones de TODOS los ficheros la viñeta «Orden de uso» es coherente: 3 niveles si el kit tiene fichero de áreas, 2 si no; 0 apariciones de «No se duplican»; (3) recuento_gate = gates.recuento_tareas.total; tareas_01 = tareas reales del fichero 01; hojas_checklist = nº de hojas de checklist del producto; fechas_bonus02 = filas con fecha entregadas en el calendario del BONUS-02 (cuenta fila a fila, sin las «(Tu fecha)»); ficheros_total; (4) ficheros de otro molde intactos (data_only sin #REF!/#NAME?); (5) barrido de vocabulario ajeno al sector en 08/09/Instrucciones (máx. 6 hallazgos nuevos). bloquea_real = true si falla (1)-(2), si un fix de la tanda 5 no está, o si hay hallazgo nuevo alto. ESCRIBE tu JSON en ${VER}/${pid}-ver4.json y devuelve el schema. ${RULES}`
}
const vers = []
for (let i = 0; i < TODOS.length; i += PAR) {
  const par = TODOS.slice(i, i + PAR)
  const r = await parallel(par.map((pid) => () => agent(verPrompt(pid), { label: `verificar:${pid}`, phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS })))
  vers.push(...r)
  log(`verificar ${par.join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit}/${x.censo_exit} idem ${x.idempotencia} niveles=${x.frase_niveles_ok} fixes=${x.fixes_tanda5_ok} rec ${x.recuento_gate} 01=${x.tareas_01} fechas=${x.fechas_bonus02} nuevos ${x.hallazgos_nuevos.length} bloquea=${x.bloquea_real}` : 'NULL')).join(' · ')}`)
}

phase('Crítico')
const CRIT = { type: 'object', properties: { listos_para_real: { type: 'array', items: { type: 'string' } }, bloqueados: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, motivo: { type: 'string' } }, required: ['pid', 'motivo'] } }, pendientes_no_bloqueantes: { type: 'array', items: { type: 'string' } }, cifras_landing: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, tareas_total: { type: 'integer' }, tareas_01: { type: 'integer' }, hojas_checklist: { type: 'integer' }, ficheros_total: { type: 'integer' }, fechas_bonus02: { type: 'integer' } }, required: ['pid', 'tareas_total', 'tareas_01', 'hojas_checklist', 'ficheros_total', 'fechas_bonus02'] } }, recomendacion: { type: 'string' } }, required: ['listos_para_real', 'bloqueados', 'pendientes_no_bloqueantes', 'cifras_landing', 'recomendacion'] }
const crit = await agent(`Crítico de completitud (tanda 5). Lee ENTEROS los 11 ${VER}/<pid>-ver4.json (pids: ${TODOS.join(', ')}), ${VER}/motor-2.4.json, ${VER}/kit-tareas-diff-firmado.json y ${VER}/critico-3.json (cruza que cada bloqueo y cada fix pedido quedó resuelto). Los 11 pids deben tener fichero; si falta, BLOQUEADO por «sin verificación». listos_para_real = kits con todo verde y sin hallazgo alto; bloqueados con celda; pendientes_no_bloqueantes (lo que queda documentado en SPEC §9); cifras_landing por kit (de los ver4: tareas_total, tareas_01, hojas_checklist, ficheros_total, fechas_bonus02) — son las que se publicarán. Escribe el informe en ${VER}/critico-4.json. Recomendación en 5 líneas: orden de ejecución real. ${RULES}`,
  { label: 'critico', phase: 'Crítico', model: 'opus', effort: 'high', schema: CRIT })
return { m, conts, vers, crit }
