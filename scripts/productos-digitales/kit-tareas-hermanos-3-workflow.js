export const meta = {
  name: 'kit-tareas-hermanos-3',
  description: 'Tanda 3 de los hermanos «▸»: motor 2.2 (sinónimos del 07, versión coherente en ficheros fuera de molde) → contenido de hotel y restaurante-creativo → verificación sonnet de los 10 kits (JSON al repo) → crítico',
  phases: [
    { title: 'Motor 2.2', detail: 'opus: decisiones (i)-(k) + regresión dry-run en serie', model: 'opus' },
    { title: 'Contenido', detail: 'opus: contenido_kit_tareas_hotel.py y contenido_kit_tareas_restaurante_creativo.py', model: 'opus' },
    { title: 'Verificar', detail: 'sonnet por kit (PAR a la vez): gates, casos §6, motor_ids, contenido aplicado; JSON al repo', model: 'sonnet' },
    { title: 'Crítico', detail: 'opus: lee los 10 JSON y decide listos/bloqueados', model: 'opus' },
  ],
}
const PAR = args && args.par ? args.par : 2
const REPO = '/Users/johnguerrero/chefpro-modernize'
const SCR = '/private/tmp/claude-501/-Users-johnguerrero-chefpro-modernize/7340312f-b4fe-4aa1-b254-4b0c17c8375f/scratchpad'
const SPEC = `${REPO}/scripts/productos-digitales/kit-tareas-v2-SPEC.md`
const R1 = `${REPO}/scripts/productos-digitales/auditorias/kit-tareas-R1.json`
const PKG = `${REPO}/scripts/productos-digitales/kit-tareas-v2_0`
const DLROOT = `${REPO}/astro-site/public/dl`
const VER = `${REPO}/scripts/productos-digitales/auditorias/kit-tareas-hermanos`
const OUT = `${SCR}/kt-hermanos-3`
const HERMANOS = ['kit-tareas-cafeteria', 'kit-tareas-pizzeria', 'kit-tareas-hamburgueseria', 'kit-tareas-dark-kitchen', 'kit-tareas-bar', 'kit-tareas-catering', 'kit-tareas-chocolateria', 'kit-tareas-heladeria', 'kit-tareas-hotel', 'kit-tareas-restaurante-creativo']
const NUEVOS = ['kit-tareas-hotel', 'kit-tareas-restaurante-creativo']
const RULES = `REGLAS DURAS: (1) PROHIBIDO modificar ${DLROOT}/** — usa \`CLAUDE_SCRATCHPAD=${SCR}/propio-<tu-rol> python3 ${PKG}/main.py --producto <pid> --dry-run [--solo motor|motor,contenido] --json ${OUT}/<pid>.json\` (nunca KIT_TAREAS_APPLY). (2) REGLA TÉRMICA ESTRICTA (el Mac se apaga a 65 °C y hoy arranca caliente): \`istats cpu temp\` ANTES de cada python; si ≥ 60 °C → \`sleep 90\` y vuelve a medir antes de lanzar; nunca dos python a la vez; nada de builds/Playwright. (3) No git add/commit/stash/clean. (4) Cita fichero:hoja:celda. (5) JSON según schema. (6) kit-tareas (el representante, en producción) NO se regenera aquí; lo que le afecte se anota para el orquestador. (7) El Mac se apagó anoche y se perdieron ficheros sin commitear: escribe tu fichero de salida (código o JSON) PRONTO en el REPO y ve actualizándolo; el scratchpad se borra en cada reinicio. (8) python3 es 3.7 con openpyxl 3.1.3: nada de f-strings con '=' ni walrus.`
const DECISIONES = `DECISIONES DEL ORQUESTADOR (tanda 3; las (a)-(h) de la tanda 2 ya están en el motor 2.1): (i) SINÓNIMOS DEL 07 en molde ▸: motor.PLANTILLA_07 / el mapa de §2.3 reconocen «Por Zona»≡«Por Área», «Por Franja Horaria»≡«Por Franja», «Por Perfil»≡«Por Perfil», de modo que diferenciar_07 y la demostración denominador-07 de §6 corren en kit-tareas-bar; las hojas P4 («Por Fase», «Por Turno») siguen fuera (P4 es mínimo por diseño). (j) VERSIÓN COHERENTE: todo fichero de un producto que tenga hoja «Instrucciones» recibe bio anclada + «Versión 2.0 · agosto 2026» aunque no tenga ni checklist ni DV (kit-tareas-catering/BONUS-02 y kit-tareas-hotel/BONUS-02); sólo texto en celdas existentes, sin tocar maquetación; si no hay hoja Instrucciones se anota en el informe y no se inventa. (k) kit-tareas-catering/07 sin contador: ACEPTADO tal cual (no se inserta fila). (f) umbral del 80 % para la remisión «→ 08/09»: se mantiene; no hay acción. (l) El hallazgo del contador P4 que contaba rótulos ya está corregido en 2.1: el verificador debe COMPROBARLO con pycel en un kit P4 (heladería o catering): hoja recién impresa → «0 de N» con N = tareas reales, sin contar rótulos.`
const BUILD = { type: 'object', properties: { ficheros_codigo: { type: 'array', items: { type: 'string' } }, pruebas: { type: 'array', items: { type: 'string' } }, afecta_al_representante: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } }, notas: { type: 'array', items: { type: 'string' } } }, required: ['ficheros_codigo', 'pruebas', 'afecta_al_representante', 'pendiente', 'notas'] }

phase('Motor 2.2')
const m = await agent(`Mantenedor de ${PKG}/ (lee motor.py y main.py ENTEROS; contenido_kit_tareas.py como referencia). SPEC: ${SPEC} (incluido §8). ${DECISIONES}\n\nAplica (i), (j) en el motor (y (l) si descubres que no está completo). Prueba en dry-run, EN SERIE: kit-tareas-bar (exit 0, 11/11, diferenciar_07 corre en sus 3 hojas y la demostración denominador-07 de §6 se ejecuta y pasa, idempotencia 0), kit-tareas-catering y kit-tareas-hotel (sus BONUS-02 con bio + Versión 2.0 en Instrucciones, resto intacto, idempotencia 0), kit-tareas-cafeteria y kit-tareas-heladeria (regresión: siguen en verde, idempotencia 0). Escribe tu informe también en ${VER}/motor-2.2.json. En afecta_al_representante repite y amplía lo que el motor 2.1 ya anotó (mensaje de la DV, texto DOM-01 del 09, protección del calendario BONUS-02) y añade lo nuevo. ${RULES}`,
  { label: 'motor-2.2', phase: 'Motor 2.2', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor 2.2: ${m ? `pendiente ${m.pendiente.length} · afecta representante ${m.afecta_al_representante.length}` : 'NULL'}`)

phase('Contenido')
const CONT = { type: 'object', properties: { pid: { type: 'string' }, fichero_codigo: { type: 'string' }, aplicados: { type: 'array', items: { type: 'string' } }, no_aplicados: { type: 'array', items: { type: 'object', properties: { tema: { type: 'string' }, motivo: { type: 'string' } }, required: ['tema', 'motivo'] } }, dryrun_exit: { type: 'integer' }, notas: { type: 'array', items: { type: 'string' } } }, required: ['pid', 'fichero_codigo', 'aplicados', 'no_aplicados', 'dryrun_exit', 'notas'] }
const contPrompt = (pid) => `Autor del módulo de CONTENIDO del kit «${pid}». SPEC del representante: ${SPEC} (§3 es el modelo; aquí el sector es el del kit). Motor: ${PKG}/motor.py (utilidades de inserción, bandas, tareas). Lee contenido_kit_tareas.py y, como ejemplos de hermanos YA hechos y aprobados en dry-run, contenido_kit_tareas_catering.py (molde P4, como el tuyo) y contenido_kit_tareas_heladeria.py: expone post(wb, fname, cambios); main.py lo importa como contenido_${pid.replace(/-/g, '_')}.py. Hallazgos de CONTENIDO de este kit (lee ENTERO): ${VER}/${pid}-verif.json → campo contenido_pendiente (altas y medias; las marcadas «(motor)» NO son tuyas) + los equivalentes del representante que apliquen al sector (orden seguro del gas; higiene personal al INICIO; anisakis si hay pescado crudo/marinado/ahumado; aceite solo en frío; mermas del día; registro diario de jornada y archivo mensual; hoja «Trimestral y Anual» de mantenimiento legal si falta; alérgenos al confirmar reserva/banquete; calendario del sector; cámaras «comprobar», no «encender»; vida útil de congelación). Lo que no aplique al sector, a no_aplicados con motivo (no inventes tareas que nadie pueda marcar). Escribe ${PKG}/contenido_${pid.replace(/-/g, '_')}.py con cambios de texto 1:1 y filas nuevas dentro del rango contado, en el vocabulario del sector. Prueba: \`CLAUDE_SCRATCHPAD=${SCR}/propio-cont-${pid} python3 ${PKG}/main.py --producto ${pid} --dry-run --json ${OUT}/${pid}-cont.json\` → exit 0, idempotencia 0; demuestra cada bloque con celda. Si el módulo añade tareas, anota en notas el recuento nuevo de tareas del kit para la landing. ${RULES}`
const conts = []
for (let i = 0; i < NUEVOS.length; i += PAR) {
  const par = NUEVOS.slice(i, i + PAR)
  const r = await parallel(par.map((pid) => () => agent(contPrompt(pid), { label: `contenido:${pid}`, phase: 'Contenido', model: 'opus', effort: 'high', schema: CONT })))
  conts.push(...r)
  log(`contenido ${par.join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit} aplicados ${x.aplicados.length} no_aplicados ${x.no_aplicados.length}` : 'NULL')).join(' · ')}`)
}

phase('Verificar')
const VS = { type: 'object', properties: { pid: { type: 'string' }, dryrun_exit: { type: 'integer' }, censo_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, listas_dv: { type: 'array', items: { type: 'string' } }, casos6_ok: { type: 'integer' }, casos6_total: { type: 'integer' }, motor_no_resueltos: { type: 'array', items: { type: 'string' } }, contenido_no_aplicado: { type: 'array', items: { type: 'string' } }, hallazgos_nuevos: { type: 'array', items: { type: 'object', properties: { severidad: { type: 'string', enum: ['alta', 'media', 'baja'] }, donde: { type: 'string' }, hallazgo: { type: 'string' } }, required: ['severidad', 'donde', 'hallazgo'] } }, tareas_total: { type: 'integer' }, ficheros_total: { type: 'integer' }, bloquea_real: { type: 'boolean' }, fichero_json: { type: 'string' } }, required: ['pid', 'dryrun_exit', 'censo_exit', 'idempotencia', 'listas_dv', 'casos6_ok', 'casos6_total', 'motor_no_resueltos', 'contenido_no_aplicado', 'hallazgos_nuevos', 'tareas_total', 'ficheros_total', 'bloquea_real', 'fichero_json'] }
const verPrompt = (pid) => `Verificador independiente del kit «${pid}». SPEC: ${SPEC}. R1: ${R1}. Verificación de la tanda 1: ${VER}/${pid}-verif.json. Motor 2.2: ${VER}/motor-2.2.json. Módulo de contenido: ${PKG}/contenido_${pid.replace(/-/g, '_')}.py (léelo: cada bloque dice qué celda toca). Regenera TÚ la copia (motor + contenido, dry-run) y: (1) exit, censo --fail, 2.ª pasada in place (diferencias), listas de DV del producto (debe ser UNA: «✓,—,N/A» en TODAS las hojas de checklist, molde ▸ y P4), casos §6 con pycel (arqueo 0/+10, contador con N/A, plantilla personalizable por fórmula, 08 precargado, bio en todas las Instrucciones, y el contador P4 recién impreso = «0 de N» sin contar rótulos si el kit tiene P4); (2) cada id MOTOR de la R1 y cada hallazgo «(motor)» de la verificación de la tanda 1 → resuelto/no; (3) cada tema de contenido_pendiente de la tanda 1 → aplicado (con celda) / no aplicado con motivo aceptable / no aplicado sin motivo; comprueba al menos 6 «aplicados» del módulo abriendo la celda real; (4) lee el kit como jefe de cocina del sector y anota hallazgos nuevos (máx. 10), incluidos textos que no cuadren con el sector, tareas duplicadas entre ficheros, horas incoherentes; (5) ficheros de otro molde NO deben quedar rotos (abre todos con openpyxl, recarga, data_only sin #REF!/#NAME?); (6) cuenta tareas_total (filas de tarea reales, sin rótulos) y ficheros_total para la landing. bloquea_real = true si el motor rompe algo, una lista de DV sigue mezclada, un caso §6 falla, hay contenido pendiente alto sin aplicar ni motivo, o hay un hallazgo nuevo alto. ESCRIBE tu JSON completo en ${VER}/${pid}-ver2.json (en el REPO) y devuelve el schema. ${RULES}`
const vers = []
for (let i = 0; i < HERMANOS.length; i += PAR) {
  const par = HERMANOS.slice(i, i + PAR)
  const r = await parallel(par.map((pid) => () => agent(verPrompt(pid), { label: `verificar:${pid}`, phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS })))
  vers.push(...r)
  log(`verificar ${par.join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit}/${x.censo_exit} idem ${x.idempotencia} dv ${x.listas_dv.length} §6 ${x.casos6_ok}/${x.casos6_total} nuevos ${x.hallazgos_nuevos.length} tareas ${x.tareas_total} bloquea=${x.bloquea_real}` : 'NULL')).join(' · ')}`)
}

phase('Crítico')
const CRIT = { type: 'object', properties: { listos_para_real: { type: 'array', items: { type: 'string' } }, bloqueados: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, motivo: { type: 'string' } }, required: ['pid', 'motivo'] } }, pendientes_transversales: { type: 'array', items: { type: 'string' } }, afecta_al_representante: { type: 'array', items: { type: 'string' } }, capa_producto: { type: 'array', items: { type: 'string' } }, recomendacion: { type: 'string' } }, required: ['listos_para_real', 'bloqueados', 'pendientes_transversales', 'afecta_al_representante', 'capa_producto', 'recomendacion'] }
const crit = await agent(`Crítico de completitud. Lee ENTEROS los 10 ficheros ${VER}/<pid>-ver2.json (pids: ${HERMANOS.join(', ')}) y ${VER}/motor-2.2.json. Cruza: los 10 pids de entrada deben tener fichero; si falta alguno es BLOQUEADO por «sin verificación». Decide: qué kits están listos para la ejecución real (exit 0, censo 0, idempotencia 0, una sola lista de DV, casos §6 completos, sin motor_no_resueltos ni hallazgos nuevos altos ni contenido alto sin aplicar), cuáles bloquean y por qué (con la celda), pendientes transversales del motor, qué debe aplicarse también al representante kit-tareas (en producción), y en capa_producto qué hay que cambiar por kit fuera de los xlsx (recuento de plantillas/tareas en la landing .ts, emails, dashboard, changelog) a partir de tareas_total/ficheros_total. Escribe el informe también en ${VER}/critico-2.json. Recomendación en 5 líneas. ${RULES}`,
  { label: 'critico', phase: 'Crítico', model: 'opus', effort: 'high', schema: CRIT })
return { m, conts, vers, crit }
