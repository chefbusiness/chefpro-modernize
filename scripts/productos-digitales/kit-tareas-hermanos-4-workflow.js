export const meta = {
  name: 'kit-tareas-hermanos-4',
  description: 'Tanda 4 de los hermanos «▸»: motor 2.3 (T-01 marco del 08, T-02 TPV 08↔09, T-03 recuento ▸+P4, T-04 áreas reales, T-05 dedupe, T-08 frase honesta) → 6 fixes de contenido → verificación sonnet de 10 hermanos + representante en dry-run (JSON al repo) → crítico',
  phases: [
    { title: 'Motor 2.3', detail: 'opus: T-01..T-05 + T-08 con regresión dry-run en serie', model: 'opus' },
    { title: 'Contenido', detail: 'fixes por kit: bar, heladería (sonnet), chocolatería, restaurante-creativo, dark-kitchen, cafetería', model: 'opus' },
    { title: 'Verificar', detail: 'sonnet por kit (2 a la vez): gates, B40, par TPV, recuento, contenido; JSON al repo', model: 'sonnet' },
    { title: 'Crítico', detail: 'opus: lee los 11 JSON y decide listos/bloqueados', model: 'opus' },
  ],
}
const PAR = args && args.par ? args.par : 2
const REPO = '/Users/johnguerrero/chefpro-modernize'
const SCR = '/private/tmp/claude-501/-Users-johnguerrero-chefpro-modernize/7340312f-b4fe-4aa1-b254-4b0c17c8375f/scratchpad'
const SPEC = `${REPO}/scripts/productos-digitales/kit-tareas-v2-SPEC.md`
const PKG = `${REPO}/scripts/productos-digitales/kit-tareas-v2_0`
const DLROOT = `${REPO}/astro-site/public/dl`
const VER = `${REPO}/scripts/productos-digitales/auditorias/kit-tareas-hermanos`
const OUT = `${SCR}/kt-hermanos-4`
const HERMANOS = ['kit-tareas-cafeteria', 'kit-tareas-pizzeria', 'kit-tareas-hamburgueseria', 'kit-tareas-dark-kitchen', 'kit-tareas-bar', 'kit-tareas-catering', 'kit-tareas-chocolateria', 'kit-tareas-heladeria', 'kit-tareas-hotel', 'kit-tareas-restaurante-creativo']
const TODOS = HERMANOS.concat(['kit-tareas'])
const RULES = `REGLAS DURAS: (1) PROHIBIDO modificar ${DLROOT}/** — usa \`CLAUDE_SCRATCHPAD=${SCR}/propio-<tu-rol> python3 ${PKG}/main.py --producto <pid> --dry-run [--solo motor|motor,contenido] --json ${OUT}/<pid>.json\` (nunca KIT_TAREAS_APPLY). (2) REGLA TÉRMICA ESTRICTA (el Mac se apaga a 65 °C): \`istats cpu temp\` ANTES de cada python; si ≥ 60 °C → \`sleep 90\` y vuelve a medir; nunca dos python a la vez; nada de builds/Playwright. (3) No git add/commit/stash/clean. (4) Cita fichero:hoja:celda. (5) JSON según schema. (6) El representante kit-tareas (en producción) se puede pasar por --dry-run (copia al scratchpad) pero JAMÁS en real. (7) Escribe tu fichero de salida (código o JSON) PRONTO en el REPO y ve actualizándolo; el scratchpad se borra en cada reinicio. (8) python3 es 3.7 con openpyxl 3.1.3: nada de f-strings con '=' ni walrus. (9) Los .xlsx llevan ESPACIO FINO (U+202F) y GUION NO SEPARABLE (U+2011): en código referéncialos por escape.`
const DECISIONES = `DECISIONES DEL ORQUESTADOR (tanda 4; base: ${VER}/critico-2.json y ${VER}/motor-2.2.json). T-01: motor._bloque_conecta contempla fname == negocio (y fname == caja): cuando el fichero descrito ES el marco, su línea «Estás en …» dice que es el MARCO del día y remite al de áreas como DETALLE y al de caja como CAJA; nunca nombra a otro fichero como marco. T-02 (TPV doble 08↔09, en toda la familia y en el representante): la tarea del fichero de CAJA que diga «Encender TPV / POS» (con o sin «/ datáfono») pasa a «Comprobar que el TPV está encendido y abrir turno de caja»; se implementa en el motor como regla de familia (RX sobre el fichero de caja), no en cada módulo; la hora no se toca. Además, si en el mismo kit una tarea del 01/04 (áreas/perfiles) dice literalmente «Encender TPV…» y es de caja, se deja (es otra capa) pero se registra en gates.tpv_duplicado para el informe. T-03: gates.recuento_tareas suma TODAS las hojas de checklist del producto (molde ▸ + molde P4 + plantillas con denominador por fórmula; sin rótulos), y el informe separa recuento_por_molde; ese total es el que publica la landing. T-04: la frase «(cocina, sala, barra)» y «(accesos, luces, clima, terraza)» se derivan de los nombres reales de hoja de CTX['f_areas'] y de las bandas del negocio; si no se puede derivar, se omite el paréntesis (nunca se inventa una terraza). T-05: gates.bandas_solapadas sin duplicados. T-06: SIN ACCIÓN (documentado). T-08: el umbral 0,8 se mantiene; la frase de Instrucciones «No se duplican: cada uno cubre un nivel.» pasa en los 11 ficheros de todos los kits a «Cada fichero cubre un nivel: el de negocio marca el HITO (encender, abrir, cerrar), el de áreas detalla CÓMO se hace en cada zona y el de caja lleva el DINERO. Si una tarea aparece en dos, es a propósito: una es el hito y la otra el detalle.» (mismo literal en todos, idempotente). BAR 07: se acepta que «Zona» quede vacía en verde en Por Franja Horaria / Por Perfil («General» era relleno). CATERING 07 sin contador: aceptado.`
const BUILD = { type: 'object', properties: { ficheros_codigo: { type: 'array', items: { type: 'string' } }, pruebas: { type: 'array', items: { type: 'string' } }, afecta_al_representante: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } }, notas: { type: 'array', items: { type: 'string' } } }, required: ['ficheros_codigo', 'pruebas', 'afecta_al_representante', 'pendiente', 'notas'] }

phase('Motor 2.3')
const m = await agent(`Mantenedor de ${PKG}/ (lee motor.py y main.py ENTEROS). SPEC: ${SPEC}. Lee ${VER}/critico-2.json (pendientes_transversales_del_motor y verificaciones_propias) y ${VER}/motor-2.2.json. ${DECISIONES}\n\nAplica T-01, T-02, T-03, T-04, T-05 y T-08 en motor.py/main.py. Prueba en dry-run, EN SERIE, con istats antes de cada uno: kit-tareas-bar (exit 0, idempotencia 0; 08:Instrucciones sin contradicción B36/B40 — cita las celdas; 09 con «Comprobar que el TPV…»; frase T-08 nueva en las 11 Instrucciones), kit-tareas-dark-kitchen (T-04: el paréntesis de áreas sale de sus hojas reales, sin «terraza»), kit-tareas-heladeria (T-03: recuento_tareas ≈ 298, con recuento_por_molde), kit-tareas-hotel (recuento ≈ 636 y el TPV del 19 reescrito), kit-tareas (EL REPRESENTANTE, en dry-run: lista EXACTA de celdas que cambian respecto a producción; se esperan SOLO el mensaje de la DV, 09:'Cierre de Caja'!B10, 08:Instrucciones!B40, 09 TPV, la frase T-08 y el desbloqueo del calendario BONUS-02; cualquier otra diferencia va a «pendiente» con la celda). Escribe tu informe en ${VER}/motor-2.3.json. ${RULES}`,
  { label: 'motor-2.3', phase: 'Motor 2.3', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor 2.3: ${m ? `pendiente ${m.pendiente.length} · afecta representante ${m.afecta_al_representante.length}` : 'NULL'}`)

phase('Contenido')
const CONT = { type: 'object', properties: { pid: { type: 'string' }, fichero_codigo: { type: 'string' }, aplicados: { type: 'array', items: { type: 'string' } }, no_aplicados: { type: 'array', items: { type: 'object', properties: { tema: { type: 'string' }, motivo: { type: 'string' } }, required: ['tema', 'motivo'] } }, dryrun_exit: { type: 'integer' }, notas: { type: 'array', items: { type: 'string' } } }, required: ['pid', 'fichero_codigo', 'aplicados', 'no_aplicados', 'dryrun_exit', 'notas'] }
const FIXES = [
  { pid: 'kit-tareas-heladeria', model: 'sonnet', tarea: `Errata «manteccar»/«Manteccar» (doble c) → «mantecar»/«Mantecar» en 03-tareas-manager.xlsx:'Tareas Diarias'!B9, 04-tareas-perfiles.xlsx:'Heladero-Obrador'!B6 y B14, BONUS-01-briefing-servicio.xlsx:'Briefing Diario'!B7 (la forma correcta ya está en 02:'Mantecación'!B32). Hazlo como sustitución 1:1 de texto en el módulo (regex sobre «mantecc» insensible a mayúsculas), aplicable a cualquier celda del kit. Después barre TODO el kit (11 ficheros) buscando otras erratas evidentes (letras dobles, tildes que faltan en palabras del oficio: «pasteurizacion», «mantecacion», «abatidor», «higiene») y corrígelas por el mismo mecanismo, listando cada celda.` },
  { pid: 'kit-tareas-chocolateria', model: 'opus', tarea: `Criterio de paso/no-paso del test de templado SOLO en chocolate negro (02-partidas-produccion.xlsx:'Templado'!B18); 'Chocolate con leche' (!B26) y 'Chocolate blanco' (!B33) se quedaron con «Test de templado y registrar lote» sin criterio. Reescribe B26 y B33 con el mismo formato que B18 y los parámetros correctos de cada cobertura (leche: trabajo 29-30 °C; blanco: 28-29 °C; test: capa fina sobre papel, a 18-20 °C cristaliza en 3-5 min con brillo uniforme y sin vetas; si no, volver a fundir y retemplar). Además el DOM-09 de tu kit: 01:'Apertura'!B30 y 01:'Cierre'!B8-B9 duplican 09 y 08 sin remisión (molde P4, colapsar_duplicados no corre): reescríbelas 1:1 como detalle del obrador («Comprobar que …», con la referencia «(hito en 08/09)») sin borrar filas.` },
  { pid: 'kit-tareas-restaurante-creativo', model: 'opus', tarea: `07-chefs-table-eventos.xlsx:'Cenas Especiales' tiene la sección nueva «AL CONFIRMAR LA RESERVA» (fila 15, alérgenos por escrito) y 9 filas más abajo la tarea vieja «Alérgenos y preferencias confirmados» (fila 24) sin retirar: dos controles para el mismo hecho. Funde: la vieja pasa a ser el control de SERVICIO («Cotejar en el pase los alérgenos confirmados por escrito con la comanda de cada comensal») o se vacía si tu motor de filas lo permite sin romper el contador. Después revisa TODAS las inserciones de _insertar_seccion() del módulo buscando el mismo patrón (tarea nueva + tarea vieja equivalente a pocas filas): lista cada caso y resuélvelo. DOM-07 (09-plantilla-personalizable.xlsx con Plantilla A/B/C vacías): fuera de alcance del motor; desde el módulo, pon en cada plantilla 3 filas de EJEMPLO en gris/cursiva con el vocabulario del kit (menú degustación, chef's table, I+D) si la hoja tiene celdas libres dentro del rango contado; si no, anótalo en no_aplicados.` },
  { pid: 'kit-tareas-dark-kitchen', model: 'opus', tarea: `Su 08-apertura-cierre-negocio.xlsx habla de terraza, sala y reservas en un negocio 100 % delivery ('Apertura del Negocio'!B11/B13/B16/B17, 'Cierre del Negocio'!B7). Añade _f08 al módulo con sustituciones de texto 1:1 (mismo nº de filas, sin tocar D/E) al vocabulario del sector: zona de recogida de riders, tablets/impresoras de plataformas (Glovo, Uber Eats, Just Eat), packaging, etiquetado, tiempos de preparación publicados; el gate negocio_precargado (firma 2 checklists con Notas, 0 huecos) debe seguir verde. Comprueba en el dry-run que el motor NO revierte tus textos en la 2.ª pasada (idempotencia 0).` },
  { pid: 'kit-tareas-cafeteria', model: 'opus', tarea: `Espresso doble: 01:'Apertura Barra Café'!B6/E6 hace la rutina completa de arranque a las 06:45 y 08:'Apertura del Negocio'!B20/D20 vuelve a mandar «encender» a las 07:15; en el cierre 01:'Cierre Barra Café'!B6-B11 hace el backflush completo y apaga, y 08:'Cierre del Negocio'!B18 manda purgar y limpiar el grupo otra vez. Reescribe en el módulo las dos tareas del 08 como HITO que remite al detalle («Comprobar que la máquina de espresso está en temperatura y purgada (rutina completa en 01)» / «Comprobar que el grupo de café queda limpio y apagado (backflush en 01)»), sin tocar horas ni filas. El TPV 08↔09 ya lo resuelve el motor 2.3: NO lo toques, solo comprueba en el dry-run que el 09 dice «Comprobar que el TPV está encendido y abrir turno de caja».` },
  { pid: 'kit-tareas-bar', model: 'opus', tarea: `Tras el motor 2.3 comprueba en dry-run: 08:Instrucciones sin contradicción (B36 vs B40), 09 TPV reescrito, 07 con sus secciones propias (Por Franja Horaria / Por Zona / Por Perfil) y «Zona» vacía en verde en las dos primeras (decisión: aceptado). Desde tu módulo: DOM-23 del verificador (lee ${VER}/kit-tareas-bar-ver2.json → motor_no_resueltos y hallazgos_nuevos) — lo que sea de CONTENIDO, resuélvelo con sustitución 1:1; lo que sea de motor, a no_aplicados con el id. Barre además el kit como jefe de barra: erratas y vocabulario (coctelería, no cocina).` },
]
const contPrompt = (f) => `Autor/mantenedor del módulo de CONTENIDO ${PKG}/contenido_${f.pid.replace(/-/g, '_')}.py del kit «${f.pid}» (léelo ENTERO antes de tocarlo; el motor es ${PKG}/motor.py, ya en 2.3). SPEC: ${SPEC}. Informe del verificador de la tanda 3: ${VER}/${f.pid}-ver2.json (campos hallazgos_nuevos y contenido_no_aplicado). TAREA: ${f.tarea}\n\nRegla de edición: cambios de texto 1:1 y filas nuevas SOLO dentro del rango contado; nunca cambies maquetación; todo idempotente (2.ª pasada 0 diferencias). Prueba: \`CLAUDE_SCRATCHPAD=${SCR}/propio-cont-${f.pid} python3 ${PKG}/main.py --producto ${f.pid} --dry-run --json ${OUT}/${f.pid}-cont.json\` → exit 0, idempotencia 0; demuestra cada cambio abriendo la celda resultante. ${RULES}`
const conts = []
for (let i = 0; i < FIXES.length; i += PAR) {
  const par = FIXES.slice(i, i + PAR)
  const r = await parallel(par.map((f) => () => agent(contPrompt(f), { label: `contenido:${f.pid}`, phase: 'Contenido', model: f.model, effort: 'high', schema: CONT })))
  conts.push(...r)
  log(`contenido ${par.map((f) => f.pid).join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit} aplicados ${x.aplicados.length} no_aplicados ${x.no_aplicados.length}` : 'NULL')).join(' · ')}`)
}

phase('Verificar')
const VS = { type: 'object', properties: { pid: { type: 'string' }, dryrun_exit: { type: 'integer' }, censo_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, listas_dv: { type: 'array', items: { type: 'string' } }, casos6_ok: { type: 'integer' }, casos6_total: { type: 'integer' }, b40_ok: { type: 'boolean' }, tpv_ok: { type: 'boolean' }, frase_t08_ok: { type: 'boolean' }, recuento_gate: { type: 'integer' }, tareas_total_manual: { type: 'integer' }, ficheros_total: { type: 'integer' }, motor_no_resueltos: { type: 'array', items: { type: 'string' } }, contenido_no_aplicado: { type: 'array', items: { type: 'string' } }, hallazgos_nuevos: { type: 'array', items: { type: 'object', properties: { severidad: { type: 'string', enum: ['alta', 'media', 'baja'] }, donde: { type: 'string' }, hallazgo: { type: 'string' } }, required: ['severidad', 'donde', 'hallazgo'] } }, bloquea_real: { type: 'boolean' }, fichero_json: { type: 'string' } }, required: ['pid', 'dryrun_exit', 'censo_exit', 'idempotencia', 'listas_dv', 'casos6_ok', 'casos6_total', 'b40_ok', 'tpv_ok', 'frase_t08_ok', 'recuento_gate', 'tareas_total_manual', 'ficheros_total', 'motor_no_resueltos', 'contenido_no_aplicado', 'hallazgos_nuevos', 'bloquea_real', 'fichero_json'] }
const verPrompt = (pid) => {
  const esRep = pid === 'kit-tareas'
  const extra = esRep
    ? `ESTE ES EL REPRESENTANTE, YA EN PRODUCCIÓN (v2.0). Regenera en dry-run (el motor es idempotente) y DIFF celda a celda contra ${DLROOT}/kit-tareas: lista CADA celda/propiedad que cambia. Se esperan SOLO: mensaje de error de la DV en las hojas de checklist, 09:'Cierre de Caja'!B10 (texto facturado), 08:Instrucciones (T-01, frase T-08), 09 TPV («Comprobar que el TPV…»), frase T-08 en las 11 Instrucciones, desbloqueo de las 22 filas del calendario BONUS-02. Cualquier otra diferencia (texto, fórmula, formato, DV, protección, alturas) es hallazgo ALTA con la celda. bloquea_real = true si hay una sola diferencia inesperada.`
    : `Verificación previa: ${VER}/${pid}-ver2.json y módulo ${PKG}/contenido_${pid.replace(/-/g, '_')}.py (léelo: cada bloque dice qué celda toca). Comprueba cada tema de contenido_no_aplicado y hallazgos_nuevos de la tanda 3 → resuelto (celda) / no.`
  return `Verificador independiente del kit «${pid}». SPEC: ${SPEC}. Motor 2.3: ${VER}/motor-2.3.json. Crítico de la tanda 3: ${VER}/critico-2.json (tu kit en tabla_por_kit y bloqueados). ${extra} Regenera TÚ la copia (dry-run) y comprueba: (1) exit, censo --fail, 2.ª pasada in place (diferencias), listas de DV (UNA: «✓,—,N/A»), casos §6 con pycel; (2) b40_ok: en el fichero de NEGOCIO (08/10/18), Instrucciones sin contradicción sobre cuál es el marco (cita B36/B40 o equivalentes); (3) tpv_ok: en el fichero de CAJA la tarea del TPV dice «Comprobar que el TPV está encendido y abrir turno de caja» y en el de negocio sigue «Encender TPV…»; (4) frase_t08_ok: la frase nueva de T-08 en las Instrucciones de TODOS los ficheros y la vieja «No se duplican» en NINGUNO (grep de las 11/13/19 hojas); (5) recuento_gate = gates.recuento_tareas del informe, tareas_total_manual = tu recuento independiente con openpyxl (filas de tarea reales de TODAS las hojas de checklist, sin rótulos ni filas libres vacías); deben cuadrar ±2 %; (6) ficheros intactos de otro molde (sin #REF!/#NAME? en data_only); (7) lee el kit como jefe de cocina del sector: hallazgos nuevos (máx. 8) — erratas, vocabulario ajeno al sector, tareas duplicadas entre ficheros, horas incoherentes (una tarea «comprobar» antes de la que «enciende»). bloquea_real = true si falla (1)-(6), si queda un hallazgo alto sin resolver o si hay hallazgo nuevo alto. ESCRIBE tu JSON completo en ${VER}/${pid}-ver3.json (en el REPO) y devuelve el schema. ${RULES}`
}
const vers = []
for (let i = 0; i < TODOS.length; i += PAR) {
  const par = TODOS.slice(i, i + PAR)
  const r = await parallel(par.map((pid) => () => agent(verPrompt(pid), { label: `verificar:${pid}`, phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS })))
  vers.push(...r)
  log(`verificar ${par.join(' + ')}: ${r.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit}/${x.censo_exit} idem ${x.idempotencia} b40=${x.b40_ok} tpv=${x.tpv_ok} t08=${x.frase_t08_ok} rec ${x.recuento_gate}/${x.tareas_total_manual} nuevos ${x.hallazgos_nuevos.length} bloquea=${x.bloquea_real}` : 'NULL')).join(' · ')}`)
}

phase('Crítico')
const CRIT = { type: 'object', properties: { listos_para_real: { type: 'array', items: { type: 'string' } }, bloqueados: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, motivo: { type: 'string' } }, required: ['pid', 'motivo'] } }, pendientes_transversales: { type: 'array', items: { type: 'string' } }, representante: { type: 'string' }, capa_producto: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, tareas_total: { type: 'integer' }, ficheros_total: { type: 'integer' }, que_cambiar: { type: 'string' } }, required: ['pid', 'tareas_total', 'ficheros_total', 'que_cambiar'] } }, recomendacion: { type: 'string' } }, required: ['listos_para_real', 'bloqueados', 'pendientes_transversales', 'representante', 'capa_producto', 'recomendacion'] }
const crit = await agent(`Crítico de completitud. Lee ENTEROS los 11 ficheros ${VER}/<pid>-ver3.json (pids: ${TODOS.join(', ')}), ${VER}/motor-2.3.json y ${VER}/critico-2.json (para cruzar que cada bloqueo de la tanda 3 quedó resuelto o sigue). Cruza: los 11 pids deben tener fichero; si falta alguno es BLOQUEADO por «sin verificación». Decide: qué kits están listos para la ejecución real (exit 0, censo 0, idempotencia 0, una lista de DV, §6 completos, b40/tpv/t08 ok, recuento cuadra, sin motor_no_resueltos ni hallazgos nuevos altos), cuáles bloquean y por qué (celda), pendientes transversales, veredicto del representante (¿solo cambian las celdas esperadas?), y capa_producto por kit con tareas_total y ficheros_total reales para actualizar landing .ts / emails / dashboard / changelog (lee ${REPO}/astro-site/src/data/productos/ para ver qué recuento publica hoy cada landing y di qué cifra hay que cambiar). Escribe el informe también en ${VER}/critico-3.json. Recomendación en 5 líneas. ${RULES}`,
  { label: 'critico', phase: 'Crítico', model: 'opus', effort: 'high', schema: CRIT })
return { m, conts, vers, crit }
