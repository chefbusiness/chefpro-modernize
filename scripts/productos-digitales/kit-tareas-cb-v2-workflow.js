export const meta = {
  name: 'kit-tareas-cb-v2-representante',
  description: 'v2.0 de los 7 kits de tareas portados de CB según kit-tareas-cb-v2-SPEC.md — T0 censo + extensiones baratas del motor (CB-E6/E7/E8) con regresión ▸ → T2 extensiones CB-E1/E2/E3/E4/E9 + contenido del representante sushi-bar → 3 refutadores → corrección → ronda 2 → crítico. SOLO dry-run; la ejecución real (canario chef-privado, APPLY) la hace el orquestador.',
  phases: [
    { title: 'Censo', detail: 'sonnet: 5 correcciones de familia sobre los 7 kits, con fichero!hoja!celda', model: 'sonnet' },
    { title: 'Motor base', detail: 'opus: CB-E6 abort por molde, CB-E7 sin caja informativo, CB-E8 autorreferencia; regresión ▸ = 0 diferencias', model: 'opus' },
    { title: 'Motor CB + contenido', detail: 'opus ×2 en serie: CB-E1 ortografía, CB-E2 contador, CB-E3 registros, CB-E4 instrucciones por tipo, CB-E9 legibilidad; contenido_kit_tareas_sushi_bar.py', model: 'opus' },
    { title: 'Refutar', detail: 'opus ×3: itamae, técnica Excel, coherencia', model: 'opus' },
    { title: 'Corregir', detail: 'opus', model: 'opus' },
    { title: 'Ronda 2', detail: 'sonnet: id a id + gates', model: 'sonnet' },
    { title: 'Crítico', detail: 'opus: listo / no listo + orden real (canario chef-privado)', model: 'opus' },
  ],
}
const PID = (args && args.productId) || 'kit-tareas-sushi-bar'
const REPO = '/Users/johnguerrero/chefpro-modernize'
const SCR = '/private/tmp/claude-501/-Users-johnguerrero-chefpro-modernize/9a6ebdb7-5d45-4ab1-9e93-10b86cb95c42/scratchpad/cb'
const SPEC = `${REPO}/scripts/productos-digitales/kit-tareas-cb-v2-SPEC.md`
const R1 = `${REPO}/scripts/productos-digitales/auditorias/${PID}-R1.json`
const PKG = `${REPO}/scripts/productos-digitales/kit-tareas-v2_0`
const DL = `${REPO}/astro-site/public/dl/${PID}`
const DLALL = `${REPO}/astro-site/public/dl`
const AUD = `${REPO}/scripts/productos-digitales/auditorias`
const CB7 = 'kit-tareas-sushi-bar, kit-tareas-asador, kit-tareas-marisqueria, kit-tareas-panaderia, kit-tareas-food-truck, kit-tareas-tapas-bar, kit-tareas-chef-privado'
const RULES = `REGLAS DURAS: (1) PROHIBIDO modificar ${DLALL}/** (producción): todo por \`CLAUDE_SCRATCHPAD=${SCR}/propio-<rol> python3 ${PKG}/main.py --producto <pid> --dry-run [...] --json ${SCR}/<rol>-<pid>.json\` (lee la cabecera de main.py para las opciones reales); nunca KIT_TAREAS_APPLY. (2) El motor ${PKG}/motor.py SOSTIENE 11 KITS EN PRODUCCIÓN: toda extensión se prueba en dry-run sobre kit-tareas-cafeteria y kit-tareas-hotel y el resultado debe ser IDÉNTICO celda a celda (valor, fórmula, DV, formato, locked) a lo que hoy hay en ${DLALL}/<pid>/ — 0 diferencias es gate bloqueante (§7-bis.24). (3) TÉRMICA (el Mac se apaga a 65 °C): \`istats cpu temp\` antes de cada python; ≥ 60 °C → sleep 90; UN python cada vez; sin builds/Playwright/navegador. (4) Nada de git commit/push/checkout/reset/stash. (5) Cita fichero!hoja!celda o fichero:línea; hallazgos TODOS, sin recortar; devuelve el schema completo. (6) Nada de cifras ni citas legales sin fuente: las de §7-bis (anisakis −20 °C ≥ 24 h / −35 °C ≥ 15 h; cocción ≥ 70 °C, aves 75; caliente ≥ 65 °C RD 3484/2000; SMI 17.094 €) van con la norma en celda de nota. (7) Marca: chef-privado «AI Chef Pro», los otros seis «ChefBusiness Consultoría Gastronómica» (acentuada, no rebrandeada).`
const BUILD = { type: 'object', required: ['rol', 'ficheros_codigo', 'secciones_spec_cubiertas', 'pruebas', 'dudas', 'pendiente'], properties: { rol: { type: 'string' }, ficheros_codigo: { type: 'array', items: { type: 'string' } }, secciones_spec_cubiertas: { type: 'array', items: { type: 'string' } }, pruebas: { type: 'array', items: { type: 'string' } }, dudas: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } } } }

phase('Censo')
const CENSO = { type: 'object', required: ['por_kit', 'total_celdas', 'notas'], properties: { por_kit: { type: 'array', items: { type: 'object', required: ['pid', 'molde', 'hallazgos'], properties: { pid: { type: 'string' }, molde: { type: 'string' }, hallazgos: { type: 'array', items: { type: 'object', required: ['correccion', 'celda', 'actual'], properties: { correccion: { type: 'string' }, celda: { type: 'string' }, actual: { type: 'string' } } } } } } }, total_celdas: { type: 'integer' }, notas: { type: 'array', items: { type: 'string' } } } }
const censo = await agent(`Censo transversal (T0.1 de §9 de ${SPEC}) sobre los 7 kits CB (${CB7}) en ${DLALL}/<pid>/: para cada una de las 5 correcciones de FAMILIA de §2.2 (cita del anisakis, temperaturas con criterio único §7-bis, frecuencias 07 vs BONUS-02, hoja de mermas/arqueo prometidos y ausentes, tildes/ñ) lista CADA celda afectada con fichero!hoja!celda y el texto actual. Solo lectura con openpyxl, un python cada vez. Escribe ${AUD}/kit-tareas-cb-v2-censo.json y devuelve el schema. ${RULES}`,
  { label: 'censo-cb', phase: 'Censo', model: 'sonnet', effort: 'medium', schema: CENSO })
log(`Censo: ${censo ? `${censo.total_celdas} celdas en ${censo.por_kit.length} kits` : 'NULL'}`)

phase('Motor base')
const mb = await agent(`Extensiones BARATAS del motor de familia (T0.2 de §9 de ${SPEC}): implementa CB-E6 (MoldeDesconocido → abortar con mensaje; hoy tapas-bar sale «en alcance 0/11» y el censo da verde), CB-E7 (CTX['sin_caja']: kit sin 08/09 de caja → gate NEGOCIO informativo, no exit 1) y CB-E8 (f_areas nunca puede remitirse a sí mismo: 01!Instrucciones!B35 de sushi-bar se autocita) en ${PKG}/motor.py y, si hace falta, ${PKG}/main.py. Lee ANTES la cabecera y el flujo de main.py/motor.py y el kit-tareas-v2-SPEC.md. Verificación exigida (§9 T0.3): dry-run --solo motor sobre ${PID} → B35 ya no se autocita, gate NEGOCIO informativo, exit 0; sobre kit-tareas-tapas-bar → ABORTA por molde; y REGRESIÓN: dry-run sobre kit-tareas-cafeteria y kit-tareas-hotel comparado celda a celda (valor, fórmula, DV, number_format, locked, altura de fila) contra ${DLALL}/<pid>/ → 0 diferencias (escribe el comparador como ${PKG}/regresion.py reutilizable). Informe en ${AUD}/kit-tareas-cb-v2-motor-base.json. ${RULES}`,
  { label: 'motor-base', phase: 'Motor base', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor base: ${mb ? `pendiente ${mb.pendiente.length} dudas ${mb.dudas.length}` : 'NULL'}`)

phase('Motor CB + contenido')
const mcb = await agent(`Extensiones del motor para la sub-familia CB (T2.1 de §9 de ${SPEC}; lee §1 y §7-bis ENTEROS): CB-E1 ortografía (paso PROPIO después del motor —hoy el motor reinyecta el texto sin tilde al reescribir Instrucciones—: diccionario + contexto sobre TODAS las celdas de texto y nombres de pestaña, sin tocar fórmulas/rutas/códigos; gate «palabras sin tilde conocidas» = 0), CB-E2 (crear contador donde geometria() devuelve contador=None: 09-plantilla-personalizable), CB-E3 (molde REGISTRO: fila_registro_appcc con fechas, DV, CF de rango, protección y print_area para 03-anisakis y 03-trazabilidad), CB-E4 (bloque «Cómo personalizar» por TIPO de entregable), CB-E9 (anchos/wrap/alturas y paleta del CF) y, si la SPEC lo incluye en esta tanda, CB-E5 (molde PLANO completo con hoja Instrucciones nueva, §7-bis.16). Todo por molde/estructura, nunca por posición fija. REGRESIÓN obligatoria con ${PKG}/regresion.py sobre kit-tareas-cafeteria y kit-tareas-hotel → 0 diferencias. Prueba en dry-run sobre ${PID} (exit 0, idempotencia 0) y sobre kit-tareas-tapas-bar (molde PLANO: si CB-E5 entra, exit 0; si no, abort limpio). Informe en ${AUD}/kit-tareas-cb-v2-motor-cb.json. ${RULES}`,
  { label: 'motor-cb', phase: 'Motor CB + contenido', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor CB: ${mcb ? `pendiente ${mcb.pendiente.length}` : 'NULL'}`)
const cont = await agent(`Contenido del representante ${PID} (T2.2 de §9 de ${SPEC}; lee §2 ENTERO, el censo ${AUD}/kit-tareas-cb-v2-censo.json y el R1 ${R1}): escribe ${PKG}/contenido_${PID.replace(/-/g, '_')}.py con el contrato que usan los contenido_<pid>.py existentes (mira dos, p. ej. cafetería y hotel) — las 39 correcciones de contenido de §2.1 (anisakis con la norma correcta y un solo límite crítico; cadena horaria del arroz posible; pH con tiras 4,0-5,0 + acidificación medida §7-bis.20; 14 alérgenos con pescado; temporadas de pescado con fuente; perfil Delivery; hoja de mermas y arqueo portados §7-bis.18; frecuencias unificadas §7-bis.21; temperaturas §7-bis.19) y las 8 hojas nuevas de §2.1, con la FUENTE de cada cifra o norma en nota. Prueba: dry-run completo sobre ${PID} → exit 0, idempotencia 0, gates.ortografia 0, gates.limite_unico 0, gates.promesas 0, DV «✓,—,N/A» en todas las hojas de checklist, contador con 3 N/A → «22 de 22», 09 con 8 tareas → «x de 8». Informe en ${AUD}/kit-tareas-cb-v2-contenido-${PID}.json. ${RULES}`,
  { label: `contenido:${PID}`, phase: 'Motor CB + contenido', model: 'opus', effort: 'high', schema: BUILD })
log(`Contenido: ${cont ? `pendiente ${cont.pendiente.length}` : 'NULL'}`)

phase('Refutar')
const HALL = { type: 'object', required: ['lente', 'hallazgos', 'veredicto', 'motivo'], properties: { lente: { type: 'string' }, veredicto: { type: 'string', enum: ['listo', 'no listo'] }, motivo: { type: 'string' }, hallazgos: { type: 'array', maxItems: 35, items: { type: 'object', required: ['id', 'severidad', 'fichero', 'ubicacion', 'hallazgo', 'evidencia', 'fix'], properties: { id: { type: 'string' }, severidad: { type: 'string', enum: ['alta', 'media', 'baja'] }, fichero: { type: 'string' }, ubicacion: { type: 'string' }, hallazgo: { type: 'string' }, evidencia: { type: 'string' }, fix: { type: 'string' } } } } } }
const LENTES = [
  { key: 'dominio', pref: 'RD', lente: 'ITAMAE / jefe de cocina de sushi bar en España con 20 años: ¿imprimo esto y se lo doy a mi equipo mañana? Orden y horas reales, anisakis y temperaturas con la norma bien citada, pH verificable, alérgenos completos, temporadas correctas, delivery, mermas, arqueo, terminología en español con tildes.' },
  { key: 'excel', pref: 'RT', lente: 'TÉCNICA EXCEL con openpyxl + pycel: contadores (con N/A, con 09 relleno), DV en todas las hojas, CF, registros APPCC (fechas, rangos), protección, A4/print_area, formatos, idempotencia, caché; y REGRESIÓN: corre regresion.py sobre cafetería y hotel y exige 0 diferencias.' },
  { key: 'coherencia', pref: 'RC', lente: 'COHERENCIA + CLIENTE ESCÉPTICO: cada id del R1 asignado a §1-§2 en el mapa §8 resuelto en una celda; promesas de landing/dashboard/emails contra las hojas reales (gates.promesas); marca según §7-bis.17; ortografía; caracteres no latinos (gate-no-latinos.py --only sushi-bar).' },
]
const refs = await parallel(LENTES.map((l) => () => agent(`REFUTADOR (${l.key}) de la sub-familia CB v2.0, representante «${PID}». Tu misión es DEMOSTRAR QUE NO ESTÁ LISTO. SPEC: ${SPEC}. R1: ${R1}. Genera TU copia (dry-run completo a ${SCR}/propio-ref-${l.key}) y aplícale la lente: ${l.lente} Ids ${l.pref}-NN. Máx. 35 hallazgos con evidencia (celda y valor leído). ESCRIBE ${AUD}/kit-tareas-cb-v2-ref-${l.key}.json y devuelve el schema. ${RULES}`,
  { label: `ref:${l.key}`, phase: 'Refutar', model: 'opus', effort: 'high', schema: HALL })))
const totalRef = refs.filter(Boolean).reduce((n, r) => n + r.hallazgos.length, 0)
log(`Refutación: ${totalRef} hallazgos · ${refs.filter(Boolean).map((r) => `${r.lente.slice(0, 10)}=${r.veredicto}`).join(' · ')}`)

phase('Corregir')
const CORR = { type: 'object', required: ['resueltos', 'no_aplicados', 'dryrun_exit', 'idempotencia', 'regresion_diferencias', 'ficheros_codigo'], properties: { resueltos: { type: 'array', items: { type: 'object', required: ['id', 'donde', 'prueba'], properties: { id: { type: 'string' }, donde: { type: 'string' }, prueba: { type: 'string' } } } }, no_aplicados: { type: 'array', items: { type: 'object', required: ['id', 'motivo'], properties: { id: { type: 'string' }, motivo: { type: 'string' } } } }, dryrun_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, regresion_diferencias: { type: 'integer' }, ficheros_codigo: { type: 'array', items: { type: 'string' } }, pendientes_de_john: { type: 'array', items: { type: 'string' } } } }
const corr = await agent(`Corrector de la sub-familia CB v2.0, representante «${PID}». Lee ENTEROS ${AUD}/kit-tareas-cb-v2-ref-{dominio,excel,coherencia}.json (${totalRef} hallazgos). Para CADA id: resuelto en ${PKG}/motor.py o en el contenido (celda/línea de prueba) o no_aplicado con motivo defendible (falso positivo con evidencia, §7-bis, o «para John»). Cruce: entrada = resueltos + no_aplicados. Dry-run completo: exit 0, idempotencia 0; regresion.py sobre cafetería y hotel: 0 diferencias. Informe en ${AUD}/kit-tareas-cb-v2-correccion.json. ${RULES}`,
  { label: 'correccion', phase: 'Corregir', model: 'opus', effort: 'high', schema: CORR })
log(`Corrección: ${corr ? `${corr.resueltos.length} resueltos · ${corr.no_aplicados.length} no aplicados · regresión ${corr.regresion_diferencias}` : 'NULL'}`)

phase('Ronda 2')
const R2S = { type: 'object', required: ['listo', 'verificados_ok', 'mal_resueltos', 'no_aplicados_injustificados', 'gates', 'hallazgos_nuevos', 'motivo'], properties: { listo: { type: 'boolean' }, motivo: { type: 'string' }, verificados_ok: { type: 'array', items: { type: 'string' } }, mal_resueltos: { type: 'array', items: { type: 'object', required: ['id', 'evidencia'], properties: { id: { type: 'string' }, evidencia: { type: 'string' } } } }, no_aplicados_injustificados: { type: 'array', items: { type: 'object', required: ['id', 'por_que'], properties: { id: { type: 'string' }, por_que: { type: 'string' } } } }, gates: { type: 'object', required: ['dryrun_exit', 'idempotencia', 'censo_exit', 'regresion_diferencias', 'ortografia', 'no_latinos'], properties: { dryrun_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, censo_exit: { type: 'integer' }, regresion_diferencias: { type: 'integer' }, ortografia: { type: 'integer' }, no_latinos: { type: 'integer' } } }, hallazgos_nuevos: { type: 'array', items: { type: 'object', required: ['severidad', 'donde', 'hallazgo'], properties: { severidad: { type: 'string' }, donde: { type: 'string' }, hallazgo: { type: 'string' } } } } } }
const r2 = await agent(`Verificador de RONDA 2 de la sub-familia CB v2.0, representante «${PID}». Lee los 3 ref-*.json y ${AUD}/kit-tareas-cb-v2-correccion.json. Regenera TU copia (dry-run completo, scratchpad propio) y comprueba UNO A UNO cada id: resuelto (celda, valor) / mal resuelto / no_aplicado justificado o no. Gates: exit, idempotencia, censo-entregables --only ${PID} --fail, regresion.py sobre cafetería y hotel (0), gate de ortografía (0), gate-no-latinos (0), DV en todas las hojas, contadores correctos con N/A y con 09 relleno. Cliente recién pagado 5 min por fichero, hallazgos nuevos máx. 8. listo=true SOLO si 0 mal_resueltos, 0 injustificados, gates limpios y 0 nuevos altos. ESCRIBE ${AUD}/kit-tareas-cb-v2-ronda2.json. ${RULES}`,
  { label: 'ronda2', phase: 'Ronda 2', model: 'sonnet', effort: 'high', schema: R2S })
log(`Ronda 2: ${r2 ? `listo=${r2.listo} · mal ${r2.mal_resueltos.length} · nuevos ${r2.hallazgos_nuevos.length}` : 'NULL'}`)

phase('Crítico')
const CRIT = { type: 'object', required: ['listo_para_real', 'bloqueos', 'avisos', 'cifras_medidas', 'orden_ejecucion_real', 'recomendacion'], properties: { listo_para_real: { type: 'boolean' }, bloqueos: { type: 'array', items: { type: 'object', required: ['id', 'severidad', 'celda', 'medido', 'fix'], properties: { id: { type: 'string' }, severidad: { type: 'string' }, celda: { type: 'string' }, medido: { type: 'string' }, fix: { type: 'string' } } } }, avisos: { type: 'array', items: { type: 'string' } }, cifras_medidas: { type: 'array', items: { type: 'string' } }, orden_ejecucion_real: { type: 'array', items: { type: 'string' } }, recomendacion: { type: 'string' } } }
const crit = await agent(`Crítico de completitud de la sub-familia CB v2.0, representante «${PID}». Lee ${AUD}/kit-tareas-cb-v2-ronda2.json, la corrección, los 3 ref-*.json, los informes de motor y contenido, y el mapa §8 de ${SPEC} cruzado contra todo (¿algún id del R1 asignado a §1-§2 se perdió?). Verifica en SOLO LECTURA 6 celdas clave de la última copia dry-run (anisakis B7/registro, contador de 01 con N/A, 09 con contador, alérgenos, temporadas, marca en A1) con valor leído; que regresion.py da 0 sobre cafetería y hotel; y que el dry-run sobre kit-tareas-chef-privado (canario real de T1: contador «5 de 41» → «0 de 36», DV «✓,—,N/A») y sobre kit-tareas-tapas-bar (molde PLANO) se comportan como manda la SPEC. listo_para_real, bloqueos con celda, cifras_medidas para la capa de producto, orden_ejecucion_real (canario chef-privado con KIT_TAREAS_APPLY=1 y respaldo → sushi-bar → hermanos en serie; censo; gate offline; commit rutas explícitas; LIVE). Escribe ${AUD}/kit-tareas-cb-v2-critico.json. ${RULES}`,
  { label: 'critico', phase: 'Crítico', model: 'opus', effort: 'high', schema: CRIT })
log(`Crítico: ${crit ? `listo_para_real=${crit.listo_para_real} · bloqueos ${crit.bloqueos.length}` : 'NULL'}`)
return { pid: PID, censo, motor_base: mb, motor_cb: mcb, contenido: cont, refutacion: refs, correccion: corr, ronda2: r2, critico: crit }
