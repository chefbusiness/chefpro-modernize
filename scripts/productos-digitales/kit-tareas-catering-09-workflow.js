export const meta = {
  name: 'kit-tareas-catering-09',
  description: 'Catering: el 09 deja de ser «caja de mostrador» y pasa a «Cobros y Facturación por Evento» (decisión de John 2026-08-23) + motor 2.5 (modelo de caja por eventos, metadata subject/title en TODOS los ficheros) → verificación de catering + 4 kits P4 (solo metadata) + representante (0 diff) → crítico',
  phases: [
    { title: 'Motor 2.5', detail: 'opus: modelo_caja eventos, metadata en todo fichero, §6 liquidación; regresión', model: 'opus' },
    { title: 'Contenido', detail: 'opus: construir el nuevo 09 de catering + capa de producto (rutas, dashboard, landing, redirect)', model: 'opus' },
    { title: 'Verificar', detail: 'sonnet: catering completo · 4 P4 sólo metadata · representante 0 diff', model: 'sonnet' },
    { title: 'Crítico', detail: 'opus', model: 'opus' },
  ],
}
const PAR = args && args.par ? args.par : 2
const REPO = '/Users/johnguerrero/chefpro-modernize'
const SCR = '/private/tmp/claude-501/-Users-johnguerrero-chefpro-modernize/7340312f-b4fe-4aa1-b254-4b0c17c8375f/scratchpad'
const SPEC = `${REPO}/scripts/productos-digitales/kit-tareas-v2-SPEC.md`
const PKG = `${REPO}/scripts/productos-digitales/kit-tareas-v2_0`
const DLROOT = `${REPO}/astro-site/public/dl`
const VER = `${REPO}/scripts/productos-digitales/auditorias/kit-tareas-hermanos`
const OUT = `${SCR}/kt-catering-09`
const NUEVO = '09-cobros-facturacion-eventos.xlsx'
const VIEJO = '09-apertura-cierre-caja.xlsx'
const P4 = ['kit-tareas-hotel', 'kit-tareas-chocolateria', 'kit-tareas-heladeria', 'kit-tareas-restaurante-creativo']
const RULES = `REGLAS DURAS: (1) PROHIBIDO modificar ${DLROOT}/** — usa \`CLAUDE_SCRATCHPAD=${SCR}/propio-<tu-rol> python3 ${PKG}/main.py --producto <pid> --dry-run --json ${OUT}/<pid>.json\` (nunca KIT_TAREAS_APPLY). (2) REGLA TÉRMICA ESTRICTA (el Mac se apaga a 65 °C): \`istats cpu temp\` ANTES de cada python; si ≥ 60 °C → \`sleep 90\`; nunca dos python a la vez; nada de builds/Playwright. (3) No git add/commit/stash/clean/mv (el orquestador hace el git mv del fichero). (4) Cita fichero:hoja:celda. (5) JSON según schema. (6) Escribe tu salida PRONTO en el REPO y ve actualizándola. (7) python3 es 3.7 / openpyxl 3.1.3: sin f-strings con '=' ni walrus. (8) U+202F y U+2011 en los .xlsx: por escape en código.`
const DISENO = `DISEÑO FIRMADO POR JOHN (2026-08-23) — el 09 de kit-tareas-catering deja de modelar una caja de mostrador (fondo, cambio, Z del TPV por turno: una empresa de catering factura por EVENTO y cobra mayoritariamente por transferencia). Nuevo fichero ${NUEVO} (sustituye a ${VIEJO}; el orquestador hará el git mv y el redirect), título «Cobros y Facturación por Evento — Catering / Eventos», con estas hojas: (1) «Instrucciones» (misma plantilla de la familia: qué resuelve, cómo usar, celdas editables, contador, filas libres, protección, se conecta con, pie, bio, versión). (2) «Antes del Evento» — checklist molde ▸ (# · Tarea · Responsable · Cuándo · ✓ Completada · Firma) con ~12 tareas precargadas + 5 filas libres + contador honesto: presupuesto firmado/aceptado por escrito; nº de comensales y fecha límite de cambios confirmados; anticipo (30-50 %) cobrado y registrado con fecha y medio; forma de pago del saldo acordada (transferencia / tarjeta / efectivo) y plazo; datos de facturación del cliente (razón social, CIF/NIF, dirección, email de facturación); condiciones de cancelación y de modificación comunicadas por escrito; proveedores externos del evento (carpa, sonido, alquiler de menaje) con pedido y pago/anticipo propios; escandallo/coste del evento cerrado y margen comprobado; seguro de RC del evento comprobado (y del recinto, si lo exige); alérgenos e intolerancias del cliente recibidos por escrito (enlazado con 06); SOLO si habrá barra con cobro en efectivo: fondo de caja preparado (remite a la sección opcional). «Cuándo» en términos de D-x (D-15, D-7, D-3, D-1). (3) «Después del Evento» — checklist molde ▸ ~12 tareas: recuento de comensales reales vs contratados; extras consumidos anotados (horas extra de personal, bebidas, suplementos, roturas); cargos por roturas/incidencias comunicados; factura emitida con desglose de IVA (10 % alimentos y bebidas no alcohólicas del servicio de catering; 21 % alquileres, decoración, servicios y bebidas alcohólicas — tal y como lo trate su asesor); saldo = total − anticipo comunicado al cliente; cobro del saldo registrado (fecha, medio, referencia); conciliación con el extracto bancario; si hubo barra en efectivo: arqueo (sección opcional); valoración/reseña pedida al cliente; expediente del evento archivado (presupuesto, factura, BEO, incidencias). «Cuándo» en D+0, D+1, D+7, D+30. (4) «Liquidación del Evento» — formulario con fórmulas, una columna de importes en verde: Evento/Cliente, Fecha, Comensales contratados / reales; Presupuesto aceptado (base, sin IVA); Extras (base); Base imponible al 10 % y al 21 % (dos celdas verdes que el usuario reparte; aviso si su suma ≠ presupuesto + extras); IVA 10 %, IVA 21 % (fórmulas); TOTAL FACTURA (fórmula); Anticipo cobrado (−) (verde); Saldo tras anticipo (fórmula); Cobrado tras el evento (verde); PENDIENTE DE COBRO (fórmula, ámbar por CF si > 0); Fecha de vencimiento del saldo (verde) y ESTADO (fórmula: «Cobrado» si pendiente ≤ 0,01; «VENCIDO» si pendiente > 0 y hoy > vencimiento; si no «Pendiente»). Debajo, sección opcional «Solo si hubo barra con cobro en efectivo»: la tabla de recuento por denominaciones (500 € … 0,01 €) con TOTAL EFECTIVO, fondo (−) y efectivo neto, que alimenta «Cobrado tras el evento» SOLO si el usuario lo decide (no enlazar automáticamente: se explica en Instrucciones). (5) «Registro de Eventos» — una fila por evento (25 filas + TOTALES): Fecha · Evento / Cliente · Comensales · Base (presupuesto + extras) · Total factura (fórmula con IVA medio o introducido) · Anticipo · Cobrado · Pendiente (fórmula) · Medio de pago · Vencimiento · Estado (fórmula Cobrado/Pendiente/VENCIDO). Fila TOTALES con sumas y nº de eventos pendientes/vencidos. Todo con los estilos de la familia (copia cabeceras, anchos, verde de edición, CF, protección sin contraseña, A4, pie del kit, contador). Vocabulario: catering/eventos, nunca «local», «turno de caja», «Z del TPV» salvo en la sección opcional.`
const MOTOR = `MOTOR 2.5 (además del diseño): (m5) METADATA EN TODOS LOS FICHEROS: motor.cerrar (o el paso final de main.procesar) fija wb.properties.title (título del fichero = A1 de la primera hoja de datos o el título de Instrucciones), .subject («<Nombre del kit> · AI Chef Pro · v2.0» con el mismo formato que ya llevan los ficheros ▸ del representante — cópialo de ahí), .creator y .keywords coherentes, para CUALQUIER fichero del producto, también los P4 y los BONUS. Hoy 26 xlsx antiguos de hotel (17), catering, chocolatería, heladería y restaurante-creativo siguen con subject v1.1. (m6) MODELO DE CAJA POR EVENTOS: contexto() reconoce el nuevo 09 como papel 'cobros' (detección por cabecera: hoja «Liquidación del Evento» o «Registro de Eventos»); CTX['modelo_caja'] = 'eventos' (por defecto 'mostrador'); _bloque_conecta e instrucciones_negocio escriben la línea de ese fichero como «la FACTURACIÓN — anticipos, liquidación y cobro de cada evento» y el orden de uso «local → eventos»; tpv_de_caja y texto_facturado NO se aplican a 'cobros'; el gate §6 sustituye las demos de arqueo por «liquidacion-cuadra» (pendiente = total − anticipo − cobrado, con pycel: caso saldo 0 → Cobrado, caso pendiente con vencimiento pasado → VENCIDO) y mantiene contador-honesto sobre «Antes del Evento»; gate_tpv deja de exigir nada al 09 de catering. (m7) main.py --producto kit-tareas-catering sigue funcionando con los 11 ficheros (el nuevo 09 entra en alcance ▸ por sus dos checklists). Regresión: representante kit-tareas 0 diferencias contra producción con el digest m3 (sus ficheros ▸ ya llevan la metadata; si m5 cambiase algo ahí, anótalo y que sea idéntico al valor actual); kit-tareas-heladeria: el diff contra producción es SOLO properties (subject/title/keywords) en sus 9 ficheros P4/BONUS y 0 en celdas. SPEC: añade §10 con m5-m7 y el diseño del 09 de catering.`
const BUILD = { type: 'object', properties: { ficheros_codigo: { type: 'array', items: { type: 'string' } }, pruebas: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } }, notas: { type: 'array', items: { type: 'string' } } }, required: ['ficheros_codigo', 'pruebas', 'pendiente', 'notas'] }

phase('Motor 2.5')
const m = await agent(`Mantenedor de ${PKG}/ (lee motor.py y main.py ENTEROS) y de ${SPEC}. ${DISENO}\n\n${MOTOR}\n\nTu parte es el MOTOR (m5-m7) y la SPEC §10; el fichero 09 nuevo lo construye el siguiente agente con un constructor ${PKG}/construir_09_catering.py que tú puedes dejar esbozado (firma: construir(ruta_destino, ctx) → usa los estilos de motor). Para probar m6 antes de que exista el 09 real, crea en tu scratchpad un 09 mínimo con las cabeceras del diseño (hojas «Antes del Evento», «Después del Evento», «Liquidación del Evento», «Registro de Eventos») y pásalo por main.py --dry-run copiando el producto al scratchpad con ese 09 sustituido. Dry-runs EN SERIE con istats: kit-tareas (0 diff), kit-tareas-heladeria (solo properties), kit-tareas-catering con tu 09 mínimo (papel 'cobros' detectado, textos de conecta correctos en 11 Instrucciones, §6 liquidación ejecutada). Informe en ${VER}/motor-2.5.json. ${RULES}`,
  { label: 'motor-2.5', phase: 'Motor 2.5', model: 'opus', effort: 'high', schema: BUILD })
log(`Motor 2.5: ${m ? `pendiente ${m.pendiente.length}` : 'NULL'}`)

phase('Contenido')
const CONT = { type: 'object', properties: { ficheros: { type: 'array', items: { type: 'string' } }, hojas_09: { type: 'array', items: { type: 'string' } }, tareas_09: { type: 'integer' }, dryrun_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, capa_producto: { type: 'array', items: { type: 'string' } }, pendiente: { type: 'array', items: { type: 'string' } }, notas: { type: 'array', items: { type: 'string' } } }, required: ['ficheros', 'hojas_09', 'tareas_09', 'dryrun_exit', 'idempotencia', 'capa_producto', 'pendiente', 'notas'] }
const c = await agent(`Constructor del nuevo 09 de kit-tareas-catering y de su capa de producto. ${DISENO}\n\nMotor ya en 2.5 (lee ${VER}/motor-2.5.json y ${SPEC} §10; ${PKG}/motor.py tiene las utilidades de estilo, bandas, contador, DV, CF, protección, A4 y bio). (A) Escribe ${PKG}/construir_09_catering.py: construir(ruta_destino) genera ${NUEVO} desde cero con los estilos de la familia (toma como referencia visual ${DLROOT}/kit-tareas-catering/08-apertura-cierre-negocio.xlsx y el ${VIEJO} actual: fuentes, rellenos, anchos, cabecera, pie, verde de edición) y las 5 hojas del diseño; fórmulas con IFERROR; CF ámbar para PENDIENTE > 0 y rojo para VENCIDO; DV «✓,—,N/A» con el mensaje de la familia (motor.DV_ERROR); contador honesto en las dos checklists; protección sin contraseña con verdes desbloqueados; A4 (ajuste a 1 página de ancho, cabecera repetida, pie). (B) Integra en main.py/contenido_kit_tareas_catering.py el paso: si el producto es kit-tareas-catering y existe ${VIEJO} en la copia de trabajo, se genera ${NUEVO} y se ELIMINA ${VIEJO} de la copia (en producción lo moverá el orquestador con git mv; main.py debe funcionar tanto si encuentra el viejo como si ya sólo está el nuevo), y después el motor normaliza el nuevo 09 como cualquier ▸. Dry-run: \`CLAUDE_SCRATCHPAD=${SCR}/propio-cont python3 ${PKG}/main.py --producto kit-tareas-catering --dry-run --json ${OUT}/catering-cont.json\` → exit 0, idempotencia 0, §6 liquidación OK, censo 0, 11 ficheros. Abre el 09 resultante con pycel y demuestra: total factura con 10/21, pendiente, estado VENCIDO con fecha pasada, Registro de Eventos TOTALES. (C) CAPA DE PRODUCTO (solo texto, sin git): ${REPO}/netlify/functions/get-download-urls.ts (las 2 entradas 'apertura-caja' de kit-tareas-catering → '/dl/kit-tareas-catering/${NUEVO}'; conserva la clave), el dashboard ${REPO}/src/pages/KitTareasCateringDashboard.tsx (título/desc de la tarjeta de caja → «Cobros y Facturación por Evento»), la landing ${REPO}/astro-site/src/data/productos/tareas/kit-tareas-catering.ts (líneas 93, 151-152, 280 y cualquier «arqueo de caja / Z del TPV / denominaciones» → cobros por evento, anticipos, liquidación, registro de eventos; cifra de tareas: recalcula el total del kit con gates.recuento_tareas del dry-run y publícalo), la entrada 'kit-tareas-catering' de ${REPO}/src/data/productos-changelog.ts (nueva entrada 2.1 · 2026-08-23 encima de la 2.0: «El 09 pasa de caja de mostrador a cobros y facturación por evento», con 4-6 cambios), ${REPO}/netlify/functions/resend-access.ts si menciona «caja», y una línea en ${REPO}/astro-site/public/_redirects: \`/dl/kit-tareas-catering/${VIEJO}  /dl/kit-tareas-catering/${NUEVO}  301\` ANTES de la regla genérica «# Genérica». Comprueba sintaxis de cada .ts con typescript.transpileModule (hay typescript en node_modules); NO lances build. Devuelve el schema con capa_producto = lista de «fichero:línea: cambio». ${RULES}`,
  { label: 'contenido:catering-09', phase: 'Contenido', model: 'opus', effort: 'high', schema: CONT })
log(`Contenido: ${c ? `exit ${c.dryrun_exit} idem ${c.idempotencia} hojas ${c.hojas_09.length} tareas09 ${c.tareas_09} pendiente ${c.pendiente.length}` : 'NULL'}`)

phase('Verificar')
const VS = { type: 'object', properties: { pid: { type: 'string' }, dryrun_exit: { type: 'integer' }, censo_exit: { type: 'integer' }, idempotencia: { type: 'integer' }, diff_vs_produccion: { type: 'object', properties: { valor: { type: 'integer' }, dv: { type: 'integer' }, alturas: { type: 'integer' }, locked: { type: 'integer' }, properties: { type: 'integer' }, otros: { type: 'integer' } }, required: ['valor', 'dv', 'alturas', 'locked', 'properties', 'otros'] }, casos_ok: { type: 'integer' }, casos_total: { type: 'integer' }, recuento_gate: { type: 'integer' }, hallazgos_nuevos: { type: 'array', items: { type: 'object', properties: { severidad: { type: 'string', enum: ['alta', 'media', 'baja'] }, donde: { type: 'string' }, hallazgo: { type: 'string' } }, required: ['severidad', 'donde', 'hallazgo'] } }, bloquea_real: { type: 'boolean' }, fichero_json: { type: 'string' } }, required: ['pid', 'dryrun_exit', 'censo_exit', 'idempotencia', 'diff_vs_produccion', 'casos_ok', 'casos_total', 'recuento_gate', 'hallazgos_nuevos', 'bloquea_real', 'fichero_json'] }
const verCat = `Verificador independiente de kit-tareas-catering tras el rediseño del 09. ${DISENO} Regenera en dry-run (motor 2.5 + constructor) y comprueba: exit, censo --fail, idempotencia (2.ª pasada con el digest m3 + properties), 11 ficheros con ${NUEVO} y sin ${VIEJO}; abre el 09 con openpyxl y pycel: las 5 hojas del diseño, ≥10 tareas en cada checklist con Responsable y Cuándo, contador honesto «0 de N» recién impreso, Liquidación: caso A (base 10 % 3.000 + 21 % 500, anticipo 1.050, cobrado resto → pendiente 0 → «Cobrado»), caso B (cobrado 0, vencimiento 2026-01-01 → «VENCIDO»), aviso si bases ≠ presupuesto + extras; Registro de Eventos: 3 filas de prueba → TOTALES y nº de vencidos; CF ámbar/rojo presentes; DV «✓,—,N/A» única en el producto; protección sin contraseña con verdes desbloqueados; A4; bio y versión; metadata title/subject v2.0 en los 11 ficheros; Instrucciones de los 11 ficheros con la línea nueva del 09 («FACTURACIÓN — anticipos, liquidación y cobro…») y NINGUNA mención a «Z del TPV», «fondo de caja» o «turno de caja» fuera de la sección opcional del 09; la capa de producto (get-download-urls.ts, dashboard, landing, changelog, _redirects) coherente con el fichero nuevo y con sintaxis válida (transpileModule); vocabulario catering como responsable de eventos (hallazgos nuevos máx. 8). Lee el kit como jefe de eventos de catering: ¿falta algo imprescindible del ciclo de cobro (p. ej. retención de anticipo en cancelación, factura rectificativa)? bloquea_real si falla cualquier gate o hay hallazgo alto. Escribe ${VER}/kit-tareas-catering-ver5.json y devuelve el schema. ${RULES}`
const verP4 = (pid) => `Verificador de «${pid}» tras el motor 2.5 (m5 metadata). Regenera en dry-run y haz el diff COMPLETO contra ${DLROOT}/${pid} con main.digest/diff_digest (m3) + wb.properties (title, subject, creator, keywords, description). Se espera: 0 en valor/DV/alturas/locked/otros y cambios SOLO en properties de los ficheros que tenían subject v1.1 (lista cada fichero con subject antes → después). Cualquier diferencia fuera de properties = hallazgo ALTA + bloquea_real. Comprueba también que el cache de fórmulas sigue (data_only sin None en 5 fórmulas al azar) y que el subject nuevo tiene el mismo formato que el del representante ${DLROOT}/kit-tareas/01-apertura-cierre.xlsx. Escribe ${VER}/${pid}-ver5.json y devuelve el schema (casos_ok/casos_total = demos §6 del dry-run; recuento_gate = gates.recuento_tareas.total, debe ser el firmado: hotel 636, chocolatería 338, heladería 298, restaurante-creativo 477). ${RULES}`
const verRep = `Verificador del REPRESENTANTE kit-tareas tras el motor 2.5. Regenera en dry-run y diff COMPLETO contra ${DLROOT}/kit-tareas (digest m3 + properties). Se espera 0 diferencias en TODAS las categorías (incluidas properties: sus ficheros ya llevan la metadata v2.0). Cualquier diferencia = hallazgo ALTA + bloquea_real. Escribe ${VER}/kit-tareas-ver5.json y devuelve el schema (recuento_gate debe ser 491). ${RULES}`
const vers = []
const v1 = await parallel([() => agent(verCat, { label: 'verificar:catering', phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS }), () => agent(verRep, { label: 'verificar:kit-tareas', phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS })])
vers.push(...v1)
log(`verificar catering + representante: ${v1.map((x) => (x ? `${x.pid} exit ${x.dryrun_exit} idem ${x.idempotencia} diff ${JSON.stringify(x.diff_vs_produccion)} bloquea=${x.bloquea_real}` : 'NULL')).join(' · ')}`)
for (let i = 0; i < P4.length; i += PAR) {
  const par = P4.slice(i, i + PAR)
  const r = await parallel(par.map((pid) => () => agent(verP4(pid), { label: `verificar:${pid}`, phase: 'Verificar', model: 'sonnet', effort: 'high', schema: VS })))
  vers.push(...r)
  log(`verificar ${par.join(' + ')}: ${r.map((x) => (x ? `${x.pid} diff ${JSON.stringify(x.diff_vs_produccion)} bloquea=${x.bloquea_real}` : 'NULL')).join(' · ')}`)
}

phase('Crítico')
const CRIT = { type: 'object', properties: { listos_para_real: { type: 'array', items: { type: 'string' } }, bloqueados: { type: 'array', items: { type: 'object', properties: { pid: { type: 'string' }, motivo: { type: 'string' } }, required: ['pid', 'motivo'] } }, capa_producto_catering: { type: 'array', items: { type: 'string' } }, orden_git: { type: 'array', items: { type: 'string' } }, recomendacion: { type: 'string' } }, required: ['listos_para_real', 'bloqueados', 'capa_producto_catering', 'orden_git', 'recomendacion'] }
const crit = await agent(`Crítico. Lee ENTEROS ${VER}/kit-tareas-catering-ver5.json, ${VER}/kit-tareas-ver5.json, los 4 ${VER}/<pid>-ver5.json de ${P4.join(', ')}, ${VER}/motor-2.5.json. Decide listos/bloqueados (con celda). capa_producto_catering: lista de ficheros .ts/_redirects tocados y si su contenido cuadra con el 09 nuevo (ábrelos). orden_git: la secuencia exacta que debe ejecutar el orquestador (git mv del 09 viejo → nuevo ANTES o DESPUÉS de main.py real, según cómo lo implementó el constructor — léelo en main.py/contenido_kit_tareas_catering.py), y qué comprobar en LIVE (curl del redirect, md5, gate). Escribe ${VER}/critico-5.json. ${RULES}`,
  { label: 'critico', phase: 'Crítico', model: 'opus', effort: 'high', schema: CRIT })
return { m, c, vers, crit }
