# PLAN MAESTRO — Migración de aichef.pro (parte pública) a Astro

> **Estado: APROBADO — EJECUCIÓN AL 100%** (decisión de John, 2026-07-03)
> Documento canónico del proyecto. Se actualiza al cierre de cada sesión con el log de ejecución (§8).

---

## 1. Por qué (evidencia, auditoría 2026-07-03)

La parte pública actual es una SPA React 18 + Vite con client-side rendering puro. Auditoría con GSC + curl UA Googlebot:

- **La edge function `og-meta` solo cubre ~63 de las 658 URLs del sitemap.** El resto (`/usos`, `/precios`, `/en`, todos los spokes probados…) sirve el `<title>`/meta de la HOME en el HTML crudo → cientos de páginas parecen duplicados de la home en la primera ola de indexación.
- **hreflang: 0 etiquetas server-side** con 7 idiomas + redirect 302 automático por idioma. Señal internacional rota.
- **HTML crudo ≈ 2,7 KB de texto visible** (shell SPA). Todo depende del render JS (segunda ola, tardía y no garantizada).
- **Hub Consultoría Gastro Pro: "Discovered — currently not indexed, Last Crawl: NEVER"** 5 semanas tras el lanzamiento. El cluster consultor (11 págs × 7 idiomas) probablemente invisible entero.
- **~90% de los clicks son de marca** (GSC 28d: 1.384 clicks / 53,8k impr / pos 17,2). Los 102 spokes ES+EN + 75 pSEO + consultor apenas generan clicks no-marca. El techo es estructural, no de contenido.

## 2. Decisiones registradas

| # | Decisión | Motivo |
|---|---|---|
| D1 | Migrar TODA la parte pública a Astro (stack estándar del grupo) | Evidencia §1; paridad con resto de webs del grupo |
| D2 | **NO aplicar quick wins (og-meta completo, prerender, hreflang edge) a la SPA actual** | Decisión de John 2026-07-03: no invertir en algo que vamos a matar; esas mejoras nacen nativas en Astro |
| D3 | Monorepo: el proyecto Astro vive en `astro-site/` dentro de este repo | Historia única, assets compartidos, un solo lugar que proteger |
| D4 | Netlify functions y flujos de pago/acceso se migran TAL CUAL (son framework-agnósticas) | Riesgo dinero = 0 cambios de lógica |
| D5 | Partes "app" (gates post-pago, dashboards descargas, biblioteca Pro Prompts, admin) = **islands React** reutilizando componentes actuales | No reescribir lo que funciona y toca dinero |
| D6 | **Paridad de URLs 1:1** en el cutover: los 658 slugs idénticos, sitemap idéntico. Rediseño/copy "2026" DESPUÉS de estabilizar, nunca mezclado con el cambio de stack | Riesgo SEO del cutover al mínimo |
| D7 | Staging con `X-Robots-Tag: noindex` hasta el cutover | Evitar contenido duplicado durante la migración |
| D8 | Builds y verificación **SOLO en nube** (Netlify deploy previews). NUNCA `npm install`/`astro build`/Playwright en local | REGLA TÉRMICA (CPU ≤65 °C) |
| D9 | Contenido nuevo que se redacte durante la mejora "2026" → `bridge.py`; imágenes → skill `generate-images` | REGLA CAPITAL de contenidos |
| D10 | Carpintería → subagentes `sonnet`/`opus` con `model` explícito; Fable orquesta y verifica lo crítico | REGLA DE DELEGACIÓN |

## 3. Arquitectura objetivo

- **Astro 5 + @astrojs/react** (islands) + Tailwind 3 (config portada verbatim del actual para paridad visual) + @astrojs/sitemap.
- **i18n nativo de Astro**: `es` default sin prefijo + `en/fr/de/it/pt/nl` con prefijo — mismo esquema de rutas actual. hreflang generado en el layout base para TODAS las páginas desde el día 0.
- **Contenido**: los datos existentes (`use-cases-content.*.ts`, `pseo-cities.ts`, `productos-digitales-config.ts`, locales JSON) se transforman con scripts a content collections / data files de Astro. No se reescribe contenido, se vierte.
- **Meta por página nativo en build** → la edge function `og-meta` MUERE con el cutover. `lang-redirect` (302 auto-idioma) se revisa en Fase 6 (con hreflang nativo probablemente sobra o se restringe).
- **Netlify**: site nuevo de staging apuntando a este repo con `base = astro-site`. El site actual no se toca hasta el cutover.

## 4. Fases, entregables y criterios de aceptación

| Fase | Alcance | Criterio de aceptación (gate UltraCode) | Est. |
|---|---|---|---|
| **0** ✅ | Scaffolding `astro-site/`: config Astro+i18n, Tailwind portado, tokens/CSS vars, BaseLayout con canonical+hreflang, netlify.toml staging (noindex), README | Deploy staging VERDE en Netlify; curl del staging muestra HTML con title/meta/hreflang correctos | ✅ 2026-07-03 |
| **1** ✅ | Núcleo marketing: home ×7 idiomas, header/footer/nav, /mentoria-online ×7, /formacion-presencial (~~/precios, servicios, sobre~~ NO existen en la SPA) | Paridad visual y de copy con producción; HTML crudo completo por idioma | ✅ 2026-07-18 |
| **2** ✅ | Use cases: 441 URLs = 7 idiomas × (hub + 61 spokes + hub consultor) — páginas dinámicas getStaticPaths | 100% spokes con title/meta/H1/FAQ schema propios en HTML crudo; diff byte-a-byte vs datos origen | ✅ 2026-07-18 (441/441) |
| **3** ✅ | pSEO ciudades: 76 URLs = hub + 15 ciudades × 5 modifiers (1 template data-driven) | 76 URLs con paridad de slug y contenido completo server-side | ✅ 2026-07-18 (76/76) |
| **4** ✅ | Productos digitales: **44** landings consolidadas (465 componentes clónicos → **4 templates** + 44 entradas) + hub | Los **44** checkouts Stripe apuntando a los MISMOS payment links (env vars, byte-compare); QA de enlaces **44/44** + hub | ✅ 2026-07-19 (44/44) |
| **5** | Zona app como islands: gates acceso, dashboards descarga, biblioteca Pro Prompts (JWT), /admin | **Compra de prueba real end-to-end** + acceso + descarga verificados; funciones netlify intactas (0 diff) | 2-3 ses. |
| **6** ✅ | SEO nativo: sitemap en build (diff exacto vs sitemap.xml actual), OG images, redirects, revisión lang-redirect, llms.txt + **155 URLs marketing/legales descubiertas en el censo** | Diff sitemap = 0 URLs perdidas; rich results válidos (FAQ/Product/Breadcrumb) en muestras | ✅ 2026-07-19 (gate 2.278 checks) |
| **7** 🚀 | **Cutover EJECUTADO 2026-07-19**: aichef.pro sirve ASTRO (el site de prod conserva dominio/functions/env; el netlify.toml raíz construye astro-site). Validado con branch deploy previo + gates en prod | **En vigilancia GSC 2-4 semanas** (hasta ~2026-08-16): cobertura ≥ baseline, 0 errores 404/soft-404 nuevos | flip ✅ 2026-07-19 |
| **8** (post) | Mejora "2026": rediseño/copy/contenido enriquecido, quick wins CTR (titles striking distance §5), más free tools | Cada mejora con research+SERP previo (REGLA CAPITAL) | abierta |
| **8B** ✅/⏳ | **Migración del blog** a `aichef.pro/blog` DENTRO de Astro. **ES COMPLETO** (8B.5, cutover del subdominio el 2026-07-19: 322 posts + 301 1:1). **EN COMPLETO en código** (8B.6, 2026-07-28: 39 de 89 posts en `/en/blog`, taxonomía y rutas por idioma, 89 reglas 301 escritas). **Resto de idiomas (fr/de/it/pt/nl): sin blog** — decisión abierta | Censo + mapa 301 verificados por gate (`fase8b-auditar-301.py`: ES 508/508, EN 189/189, cero 301→404 y cero cadenas). ⏳ **Falta la única acción manual: `enblog.aichef.pro` como alias en Netlify + DNS** (runbook: `CUTOVER_ENBLOG_PENDIENTE.md`) | ES hecho · EN a falta de DNS |
| **8C** (post) | **Página dedicada por agente IA (~70)**: cada agente del catálogo con su página propia — captura de pantalla, vídeo demostrativo, explicación de qué hace y cómo aprovecharlo, enlaces a sus prompts + librería Pro Prompts del agente, FAQ. Template data-driven multiidioma + interenlazado (catálogo↔agente↔use cases↔prompts). Idea de John 2026-07-19 | REGLA CAPITAL: keyword research + SERP por agente ANTES de redactar; contenido con bridge.py; imágenes/capturas reales + generate-images; FAQ obligatoria; 0 páginas huérfanas | post-cutover · **la auditoría de riesgo de imagen de 8C está CERRADA (2026-07-30)**, pendiente sólo el bloque de páginas por agente |
| **8D** ⏳ | **Poda de contenido delgado del glosario**: las entradas cortas se reescriben al estándar enriquecido con `fase8d-ampliar-glosario.py` (research + SERP → bridge → tablas, FAQ al frontmatter, 2 imágenes, 3 banners). **4 hechas** (cocina molecular, food pairing, token, LLM). La mediana del glosario ha subido de 1.285 a **1.345** palabras | **Comprobar canibalización ANTES** (`ls` de slugs con el término + GSC por `page,query`): a veces el research dice que NO se amplíe, como en food pairing. Regenerar un post publicado **pisa las ediciones manuales** | ⏳ quedan **4 bajo 400 palabras** (`yuzu-kosho-condimento-japones-tendencia` 226, `mise-en-place` 274, `chile-crisp-condimento-viral-2026` 331, `coccion-a-baja-temperatura-concepto-y-definicion` 359) y **8 entre 400 y 700** |
| **9** (siguiente nivel) | **Replicar y Sustituir Pickaxe**: app propia `app.aichef.pro` (chat + agentes + créditos + MCP/actions/webhooks). Visión completa en [`REPLICAR_Y_SUSTITUIR_PICKAXE.md`](REPLICAR_Y_SUSTITUIR_PICKAXE.md) | Se itera la idea al cerrar Fase 7; no se ejecuta hasta madurar stack y spec | post-Astro |

**Total estimado (Fases 0-7): 12-18 sesiones.**

## 5. Backlog SEO que nace DENTRO de Astro (ex-quick wins, por D2)

- hreflang 7 idiomas en todas las páginas (Fase 0, layout base).
- Meta únicos por URL para las 658 (Fases 1-4, nativo).
- Titles/CTR de striking distance: `/herramientas-gratuitas` (pos 3,8 / 468 impr), `/en/kitchen-management-software-ai` (7,9 / 462), `/en/reduce-restaurant-costs-ai` (8,2 / 378), `/kit-escandallos` (14,7 / 236) → Fase 8.
- Más free tools (mejor performer no-marca) → Fase 8 con `free-tool-strategy`.
- **PENDIENTE (John, 2026-07-31): desplegar públicamente los agentes nuevos.** Hay un montón de agentes desarrollados y ya habilitados en la plataforma que **no están en el menú de la home ni en las landings públicas**: tras activarlos, el foco se puso en el hub de librerías de prompts y la exposición pública se aparcó *esperando la migración a Astro* — que es justo donde estamos. Por eso `src/data/apps.ts` sólo pinta 30 apps (24 con slug) frente a los ~56 nombrados del catálogo real, y por eso hay 28 URLs de slug opaco en el sitemap de la app sin identificar. Tarea: censo definitivo → alta en `apps.ts` con slug y categoría → menú de la home + landings → enlazado con sus páginas 8C.
- Poda blog posts fa/uz/ka/ko (skill `prune-posts`) → independiente de la migración, cualquier momento.
- Housekeeping GSC: retirar sitemap www duplicado + sitemap tienda stale → en Fase 7.
- Housekeeping GSC **pendiente (2026-07-29)**: dar de baja los 5 sitemaps de `blog.aichef.pro` (muertos desde el cutover, 1 error cada uno) y los 4 de `enblog.aichef.pro` cuando se haga su DNS. La API lo permite pero es operación destructiva desactivada por defecto → se hace en la UI.
- **Librerías de prompts (8C)**: 27 publicadas de 85 agentes. Faltan ~58 en español (12 son el bloque de hotel, que de momento solo tiene sentido en inglés) y **26 versiones inglesas**, con el adaptador escrito pero sin validar.
- **Landings de producto en inglés**: no existen. Los banners de los posts EN apuntarán a la landing ES con UTM propio para medir la fuga; crearlas es zona de dinero (Stripe) y exige su propio ciclo.
- **Indexación del sitio nuevo**: a 9 días del cutover, las archives de categoría siguen "Detectada: actualmente sin indexar" y posts del refresh Tier A son desconocidos para Google. Sitemap reenviado el 2026-07-28; el resto es enlazado interno + tiempo + solicitudes manuales de indexación.

## 6. Riesgos y mitigaciones

1. **Romper pagos/accesos** → D4+D5 (cero cambios de lógica) + compra test real en Fase 5 + los 2 bugs históricos (access gates 2026-04-29, paths descarga) como checklist explícito.
2. **Regresión SEO en cutover** → D6 paridad 1:1 + D7 noindex staging + diff sitemap Fase 6 + vigilancia GSC Fase 7. Rollback = revertir swap en Netlify (la SPA queda intacta en el repo).
3. **Térmica** → D8. Si una sesión exige iterar mucho: mover dev a Abacus (skill `migrar-a-abacus`).
4. **Doble mantenimiento durante la migración** → congelar productos nuevos y cambios de copy en la SPA salvo urgencias (pricing, bugs); si algo cambia en la SPA, anotarlo en §8 para replicar en Astro.

## 7. Toolkit de verificación (cada fase)

```bash
# Qué ve un bot (staging o prod)
curl -s -A "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)" URL | grep -o "<title>[^<]*</title>\|hreflang"
# Estado deploy Netlify staging → panel o API; GSC vía MCP gscServer; temperatura → istats cpu temp
```

## 8. Log de ejecución (actualizar cada sesión)

- **2026-08-01 cierre (8D: los 2 glosarios ultradelgados ampliados)** — Cerrada la última pendiente desbloqueada del handoff anterior. `token-unidad-inteligencia-artificial` pasa de **49 a 1.414 palabras** y `llm-large-language-model-cocina` de **78 a 1.959**; los dos con 2 tablas, 8 preguntas de FAQ (**emiten `FAQPage`, que antes no hacían**), 2 imágenes de cuerpo con destacada única y 3 banners de producto. Enlazados entre sí —el LLM predice el siguiente token— más 4 enlaces internos nuevos. **El research cambió el enfoque en los dos**: la SERP de «qué es un token» (1.600/mes) es de **criptomonedas y ciberseguridad** (BBVA, Wikipedia, Fortinet, Kraken) y la acepción de IA sólo asoma en las posiciones 10 y 13, así que no se pelea esa keyword y se ataca «token ia» despachando pronto la confusión, que es literalmente lo que Google pregunta en el People Also Ask; y «qué es un LLM» (4.400/mes) lo copan IBM, AWS, Cloudflare y Google Cloud, a los que no se les gana la definición genérica, de modo que el peso del post va a la capa que ninguno cubre: **qué NO sabe hacer un LLM en una cocina** (puede inventarse un gramaje, una temperatura o pasar por alto un alérgeno; el LLM propone, el profesional valida). GSC confirmó **cero canibalización**: «token» tiene 0 impresiones en 90 días en todo el dominio y «llm» tiene 1, en una URL vieja del blog inglés. **De paso, un defecto publicado la víspera**: el parser de `fase8d` no contemplaba listas y las 7 viñetas del equipamiento de `cocina-molecular` se servían como **párrafos sueltos empezados por «- », sin `<ul>`**; ahora emite `<ul class="wp-block-list">`. Y al regenerar ese post para repararlo **se perdieron sus 2 enlaces internos** (las ediciones a mano posteriores no viven en el `.txt` de bridge): repuestos, y el aviso queda en el docstring y en `CLAUDE.md`. Gates: 8B 3.549/0, enlaces vivos **194 destinos y 0 rotos**, H1 0, restos de WordPress 0, librerías EN 26/26. Build verde, 1.248 páginas. Commit `1a43285`.

- **2026-08-01 noche (8C-inglés COMPLETO, las 119 free tools pasan a SSR, limpieza del blog)** — **🔴 EL HALLAZGO GORDO: 119 páginas indexables servían CERO texto server-side.** Las 17 free tools × 7 idiomas se montaban con `client:only="react"`, así que Astro **no las renderizaba en servidor**: title, meta y JSON-LD correctos y el cuerpo vacío hasta que corriera el JS. Es literalmente la patología del §1 de este plan —la que motivó la migración entera— y ese clúster se quedó fuera del cambio sin que nadie lo detectara en nueve meses. Pasarlas a `client:load` exigió dos cosas que en el navegador se dan por supuestas: **la ubicación** (el shim del router leía `window.location` en un initializer de `useState`, que corre durante el render → «window is not defined»; ahora la página Astro la inyecta por contexto) y sobre todo **el idioma** — i18next detecta por `localStorage`/`navigator`, inexistentes en servidor, y `useLanguage` lo corrige en un `useEffect` que en SSR NO corre, así que sin fijarlo las seis versiones no españolas se habrían renderizado **EN ESPAÑOL**, contenido equivocado e indexable servido a Google en `/en/`, `/fr/`, `/de/`… Lo resuelve `MarketingShell`. Dos bloqueos más del build: `AnnouncementBar` leía `localStorage` durante el render, y `jspdf`/`xlsx` (CommonJS) rompen el loader ESM de Node → `vite.ssr.noExternal`. **NO se tocó la zona app**: sus 89 páginas siguen con `client:only` porque es contenido de PAGO y renderizarlo en servidor lo metería en el HTML público (verificado: 0 de 89 emiten texto). Verificado con Playwright: 0 avisos de hidratación en ES/EN/DE/NL, el idioma aguanta tras hidratar y **la URL gana a la caché de localStorage**. **8C-inglés cerrado: 26 de 26 librerías**, gate propio 26/26 sin errores, más el hub `/en/prompt-libraries` (índice curado; NO es el catálogo completo como el español porque el repo no tiene verificados los nombres ingleses de los ~51 agentes sin librería ni de los módulos — deducirlos es el error que ya costó cuatro correcciones). **Limpieza del blog**: fuera el widget «También te puede interesar» **congelado** de WordPress en 15 posts (duplicaba los relacionados del layout, metía 7-8 `<h2>` falsos por post, enlazaba a 3 páginas 404 y en 4 posts ni tenía título), **10 enlaces internos muertos** repuntados, y **26 `<h1>` duplicados** eliminados o degradados. **Estado final: 1.121 URLs indexables y NINGUNA sin texto server-side; 1.123 páginas con exactamente un `<h1>` y ninguna con dos.** **Contenido**: los 2 glosarios delgados ampliados con research previo, y el research CAMBIÓ EL PLAN en uno de ellos — `cocina molecular` (880 búsquedas/mes, 106 impresiones, una sola página compitiendo) se amplió de 352 a 1.437 palabras, pero `que-es-el-food-pairing` resultó ser una trampa: existe un `manual-del-food-pairing` de 3.643 palabras cuyo primer h2 es literalmente «¿Qué es el Food Pairing?» y aun así el glosario de 421 palabras rankea MEJOR que él (pos. 41 y 9 frente a 84), así que inflarlo habría enfrentado las dos páginas por la misma keyword; se amplió a 938 palabras como PÁGINA DE DEFINICIÓN, cediendo la profundidad al manual con enlace explícito. Los dos emiten ahora `FAQPage`, que antes no hacían (su FAQ eran encabezados sueltos en el cuerpo). Prosa con bridge.py, 4 imágenes con Nano Banana, 3 banners por post. **Tercer resto congelado de WordPress**: el CTA de newsletter a un 404 que además afirmaba «+2.500 chefs ya reciben nuestro contenido exclusivo» sobre una newsletter cuyo formulario está desconectado en el código, y el bloque `wp-block-jetpack-donations` en **35 posts** pidiendo donaciones en el blog de un SaaS, con botones «Donar» SIN href (105 encabezados falsos). Van tres familias de resto (relacionados, donaciones, newsletter): asumir que habrá una cuarta. Cinco gates nuevos: `fase8c-libreria-en-gate.py`, `fase8c-h1-unico.py`, `fase8c-quitar-relacionados.py`, `fase8c-restos-wordpress.py`, `fase8c-enlaces-vivos.py` (este último cerró con **191 destinos y 0 rotos**). **⏳ Bloqueado esperando a John**: el listado inglés completo (77 agentes + nombres de los 15 módulos) para que el hub EN sea el catálogo entero, y los 2 glosarios ultradelgados que quedan (`token-…` con 49 palabras, `llm-…` con 78). Handoff: `SESSION_HANDOFF_2026-08-01-B.md`.

- **2026-07-31/08-01 (8C ARRANCA: 2 librerías nuevas, hub reconstruido, tablas arregladas, banners de producto y salto al inglés — con 4 correcciones de John sobre el catálogo)** — **Contenido**: librerías de `ID Alérgenos` y `Comida de Personal` (105 prompts cada una, 7 bloques × 15, FAQ en frontmatter que SÍ emite FAQPage —los 25 heredados la tienen solo en el cuerpo y por eso ninguno lo emitía—, 3 imágenes propias). Generador reutilizable `fase8c-libreria-assemble.py` con caché por sección. **Hub `/libreria-de-prompts` reconstruido**: la migración lo había degradado a un 301 hacia la archive cronológica (marcado como «interino hasta hub 8C», deuda nunca pagada); ahora es página propia con **85 agentes en los 15 módulos reales de la plataforma**, y el 301 del subdominio y el enlace del pie apuntan ahí. **🐛 Las tablas de prompts escondían el 72 % del contenido** tras un scroll horizontal (2.205 px fuera de pantalla en escritorio, 88 % en móvil): medido con Chromium headless y arreglado en dos capas (global + `.tabla-prompts` con tarjetas en móvil) sobre 177 tablas de 26 posts. **81 banners de productos digitales** en 27 posts (política nueva de John: mínimo 3 por post a tres alturas; son pago único, no la suscripción, y son la línea más desatendida), con datos leídos del catálogo real y UTM por post. **Marca**: canal `@aichefpro` en el pie y en la columna de Formación (7 idiomas), nombre de John enlazado a johnguerrero.es en las 361 firmas y en JSON-LD, `sameAs` de la Organization cuadrado con los perfiles reales. **Research**: DataForSEO operativo; Keywords Everywhere y Brave MUERTAS (volumen 0 hasta para «recetas»). Dato que reordena prioridades: la intención «prompts» tiene **volumen cero** en España — estos posts son **valor agregado para quien ya usa la plataforma**, no captación, y así se miden (criterio de John). **⚠️ CUATRO ERRORES MÍOS que corrigió John, todos con la misma raíz —crucé `apps.ts`, el sitemap de la app y `linkify-use-case.tsx` creyendo que describían el producto, y son vistas parciales y viejas—**: borré del hub la categoría de hotelería «por vacía» (existe, 12 agentes, en ambos idiomas y con nombres en inglés porque es la lengua franca del F&B hotelero); publiqué 3 agentes inexistentes; nombré mal los 27 recetarios (son «Cocina Mexicana», no «Mexicana» — **CLAUDE.md afirmaba lo contrario y era falso, ya corregido**); e iba a publicar los 27 posts ingleses con los nombres españoles de los agentes, cuando la plataforma inglesa usa `Allergen ID`, `Waste GenCal`, `Staff Meal`, `Avant-garde Cuisine`… **Fuente autorizada desde ahora: los listados de Pickaxe (86 agentes ES / 77 EN), volcados en `fase8c-agentes/`.** **EN A MEDIAS**: `fase8c-libreria-en.py` (adaptador a EE. UU., no traductor) está completo pero **nunca se ha ejecutado de principio a fin**; el piloto quedó corriendo con las piezas en `/tmp` y ningún `.md` escrito. Decisiones para el inglés: EE. UU. por defecto con guiño a UK/AU/NZ/India nombrando solo al regulador; banners a la landing ES de momento; `Chef Privado Pro` fuera porque no existe en inglés. Handoff: `SESSION_HANDOFF_2026-08-01.md`.

- **2026-07-30 (auditoría de riesgo de imagen del blog CERRADA)** — Fase 2 del escáner que quedó abierta tras cazar la imagen con parecido a Gordon Ramsay. **1.096 imágenes** (las que publican los 361 posts, ES y EN) → **477 con cara** → analizadas una a una. **Fue un incidente aislado, no un patrón: sólo 2 críticos**, los dos imagen DESTACADA y los dos ya sustituidos con Nano Banana 2 (sin caras, sin marcas, sin texto), con `modDate` y `blog-lastmod.json` regenerados y verificación live (nuevas 200, retiradas 404, `og:image` correcto). Uno era un **nombre propio inventado bordado en la chaquetilla con los glifos rotos** —persona ficticia presentada como real—, no un parecido; el otro lo marcó el modelo como Javier Bardem con confianza alta pero **al abrirlo no hay parecido inequívoco**: se retiró igualmente por ser cara en primer plano como destacada, dejando constancia de la discrepancia. **Decisión de John: los 14 retratos posados SE QUEDAN** (incumplen la regla de marca del pipeline pero no son riesgo legal). Los 159 "marca de tercero" se dejan también: son ruido (logo de Apple en un portátil, capturas con Google/WordPress, un horno RATIONAL) o **fotos legítimas del entorno de John** (Diego Schattenhofer, 1973 Taste, Michelin, Repsol). **Dos fallos del propio escáner arreglados**: sólo miraba `content/blog/es/`, así que las 93 imágenes del blog EN no se habían escaneado NUNCA, y daba por limpias las que fallaban con error de API; además tiene modo `--informe` que ordena por gravedad y localiza cada imagen en su post. Handoff: `SESSION_HANDOFF_2026-07-30.md`.

- **2026-07-28/29 (cierre de 8B.6 + AUDITORÍA del mapa 301: 24 familias de URL que morían en un 404)** — Continuación de la sesión de 8B.6. **Cableado de la navegación EN**: Header/Footer/Hero llevaban el hub del blog a `/blog` fijo (el Header con un `lang === 'en' ? '/blog' : '/blog'`), así que el lector inglés salía del artículo y aterrizaba en español → `blogHubHref(lang)`, que NO es `blogBase(lang)`: /fr/blog, /de/blog… no existen y habría cambiado un enlace en idioma ajeno por un 404. La barra de anuncios EN, que promocionaba un post español, apunta ya a `/en/blog/best-ai-tools-for-chefs-2026` con copy nuevo en `en.json` (compartido con la SPA → cambiados los dos sitios que la pintan). **EL HALLAZGO GORDO**: al escribir la batería del cutover EN se descubrió que el WordPress sirve las archives de categoría en la RAÍZ (base de categoría vacía; el campo `link` del REST lo confirma) y **caían en la genérica `/:slug → /blog/:slug`**, que las trata como posts. Verificado contra producción: `blog.aichef.pro/ia-en-gastronomia` → 301 → 404. Auditando el censo COMPLETO del export aparecieron **24 familias rotas** (8 archives + 7 páginas WP + sitemaps hijos + robots.txt + favicon.ico + archives de año + amp/wp-login/wp-admin/xmlrpc), todas con la misma causa: **la genérica se traga TODO lo que llegue con un solo segmento**. Arreglado en los DOS mapas y en los DOS generadores (no sólo en el fichero, o la próxima regeneración se lo come), con destinos con sentido (/about→/sobre-nosotros, /contact→/contacto, sitemaps→sitemap-index) y aplanada la única cadena 301→301 en su origen (`why` de fase8b-tiers.json). **Gate nuevo y permanente: `fase8b-auditar-301.py --sitio es|en`** — simula el motor de Netlify (first match wins, `*` prefijo, `:slug` un segmento) contra el censo del export, sigue cadenas y exige que el destino exista en el dist. Estado: **ES 508/508 · EN 189/189 · 0 rotos · 0 cadenas**, y las 16 familias del ES verificadas live tras el deploy. Comprobado además que las 1.119 imágenes/audios que usan los 361 posts existen todas. **GSC (dato que corrige la narrativa)**: las URLs rotas eran ~61 impresiones y 4 clics en 90 días sobre 56.582 del subdominio, y salen como "URL desconocida para Google, nunca rastreada" → el bug era real pero su impacto medible fue ridículo; NO hay nada que revalidar. Lo que sí aparece es que **el sitio nuevo apenas está indexado**: las archives de categoría están "Detectada: actualmente sin indexar" y posts del refresh Tier A son desconocidos para Google. Sitemap-index reenviado (Google lo descargó en el acto, 0 errores); "Validar corrección" y "Solicitar indexación" NO existen en la API (sólo UI). **Respaldo**: el export del WordPress inglés (89 posts + taxonomías) existía en UNA copia (VPS) → subido al repo privado `chefbusiness/aichef-blog` (`enblog-export-2026-07-28/`, texto; medios fuera por convención). **PENDIENTE DE JOHN (confirmado por él el 2026-07-29: lo hará y avisa)**: (1) alias de `enblog.aichef.pro` en Netlify + DNS —ojo a las A records que el panel de Hostinger recrea, ver `CUTOVER_ENBLOG_PENDIENTE.md`—; (2) en GSC, solicitar indexación de las archives + posts prioritarios y dar de baja los 5 sitemaps muertos del subdominio ES. Handoff: `SESSION_HANDOFF_2026-07-29.md`.

- **2026-07-28 (Fase 8B.6 — BLOG EN MIGRADO a /en/blog; falta sólo el DNS del subdominio)** — **Censo y criterio**: enblog.aichef.pro tenía 89 posts publicados; se migran **39** (contenido real, media 3.645 palabras, 31 clics y 7.678 impresiones en 90 días — 21 de ellos con CERO impresiones pese a ser guías de 3.000-5.000 palabras: el problema no es la pieza, es el subdominio sin autoridad) y se descartan las **50 guías de ciudad** (`local-ai-guides`, 0 clics, plantilla repetida por ciudad → importarlas sería traer doorway pages; mismo criterio Tier D que con los 50 pSEO del ES). **Arquitectura**: el blog EN NO es traducción del ES sino un árbol propio, así que `lib/blog.ts` pasa a estar indexado por idioma (`getCategories(lang)`, `getCategory(slug, lang)`) y todas las URLs salen de helpers (`postPath`/`categoryPath`/`listPagePath`/`blogBase`), única fuente de verdad; segmentos NATIVOS en inglés (`/en/blog/category|page/…`) porque el ES sólo usa `categoria`/`pagina` por herencia de WordPress y ahí hay 301 vivos e indexados que no se tocan; `locales={[lang]}` en los posts → cada uno declara sólo su hreflang + x-default a sí mismo (nunca un par ES↔EN que no existe); copy nativo (no traducido) en layout, CTA (→ enapp.aichef.pro) y descripciones de categoría; imágenes en `/blog-assets/en/` (subcarpeta propia: los nombres chocan con los del ES). **301**: bloque de 89 reglas de `enblog.aichef.pro` generado por `fase8b6-301map-en.py` (idempotente, con marcadores), con las 50 guías de ciudad listadas UNA A UNA antes de la genérica —si no, `/:slug → /en/blog/:slug` las convierte en 50 URLs 404— y sin comodines a mitad de segmento (el splat de Netlify es para el final del path y el fallo sería silencioso); verificado que las 184 líneas del mapa ES quedan byte a byte idénticas. **Cableado**: Header/Footer/Hero llevaban el hub del blog a `/blog` fijo (el Header con un `lang === 'en' ? '/blog' : '/blog'` esperando a esta fase) → nuevo `blogHubHref(lang)`, que NO es `blogBase(lang)`: /fr/blog, /de/blog… no existen y habría cambiado un enlace en idioma ajeno por un 404. **Verificación**: build limpio (`rm -rf .astro dist`) 1.216 páginas, 42 URLs EN en el sitemap con lastmod real, canonical/hreflang correctos, cero fuga de posts EN en el hub y las archives ES, y /fr y / (ES) idénticos. **PENDIENTE (acción de John)**: añadir `enblog.aichef.pro` como alias del site en Netlify + DNS para que el mapa 301 se ejecute; y decidir a qué pieza EN apunta la barra de anuncios, que en los 6 idiomas no-ES sigue promocionando un post español. También queda abierta la auditoría 8C de riesgo de imagen (`fase8c-escanear-caras.py`: fase 1 pasada sobre 1.003 imágenes, fase 2 sin correr).

- **2026-07-19/20 (noche — 🚀 CUTOVER 8B.5 EJECUTADO + refresh Tier A 78/134 + F8 precios ×7 + páginas E-E-A-T)** — **8B.5 CUTOVER DEL SUBDOMINIO COMPLETO**: causa raíz del atasco DNS resuelta por John (blog era DOMINIO PRINCIPAL nativo de la instancia WP, apaño manual de Hostinger de hace 2 años → el panel recreaba las A records; John desvinculó el subdominio y aparcó el WP en `darkgrey-dugong-825343.hostingersite.com` como backup vivo). CNAME `blog`→`aichefpro.netlify.app` live, cert SSL emitido solo por Netlify en minutos, **batería 301 7/7 verde** (/, post, category→categoria, /feed→rss.xml, wp-content→blog-assets, /fa/*→/blog), vigilancia ampliada con 3 checks 301+TLS del subdominio. ⚠️ **Hostinger NO se cancela hasta ~2026-07-24** (margen rollback DNS; yo doy la señal). **Refresh Tier A slices A4-A8 (50 posts, 78/134 = 58%)**: misma receta Workflow (productor opus + QA adversarial opus por post, WebSearch SERP + bridge.py + fase8b-refresh-assemble); QA reparó >100 issues pre-publicación — 4 agentes INVENTADOS (verificación contra `src/lib/linkify-use-case.tsx` = catálogo canónico), 7º idioma "catalán"→neerlandés, feature fantasma (editor de fotos reales), IAE 671.5→672, dato EFSA recaracterizado contra fuente, RD 315/2025 verificado real contra BOE (BOE-A-2025-7659); cada slice: greps prohibidas + regen blog-lastmod.json + gate 8B verde + push + vigía deploy + spot-check live. **F8**: `/precios` + `/en/pricing` + **5 idiomas nuevos** (/fr/tarifs /de/preise /it/prezzi /pt/precos /nl/prijzen; workflow 5 prod + 5 QA nativos, hreflang ×8 verificado en las 7, Header "Precios"→página dedicada, Footer mapa 7 idiomas) + **/contacto /sobre-nosotros /faq** (cierran los 404 del llms.txt; E-E-A-T: bio John, estándares editoriales, FAQPage) + 2ª imagen de que-significa-abocar. Sitemap **696→1.073 URLs** con lastmod real; reenviado a GSC. **Fix de cierre**: auditoría refs↔disco↔tracking cazó 9 imágenes de productores sin commitear (404 live) → `2b6786b`. Gotchas nuevos: zsh `path` como variable de bucle PISA $PATH; `grep -c` en HTML minificado cuenta 1 línea (usar python count); los commits quirúrgicos de slices deben incluir `astro-site/public/blog-assets/` nuevos. **Pendiente decisión John**: 301 de ~13 posts Tier A duplicados de pilares refrescados (consolidación SEO). Quedan ~27 Tier A únicos + 34 librerías→8C. Handoff: `SESSION_HANDOFF_2026-07-20.md`.
- **2026-07-19 (Fase 8B — port ES COMPLETO en rama, gate 5.627 checks verde; PENDIENTE revisión visual de John para merge)** — Misma jornada que el cutover de Fase 7. **Análisis previo** (encargo de John): `FASE8B_ANALISIS_CONTENIDO_BLOG.md` — censo live 408 posts, GTranslate con suscripción INACTIVA (402 en los 6 idiomas "conservados"; John confirmó que dejó de pagarla a propósito), los 50 pSEO despublicados en mayo estaban REPUBLICADOS en goteo desde la VM Abacus, librerías de prompts 24/74 (tracking local desfasado). **Export-seguro pre-apagado** en `~/aichef-blog/export-2026-07-19/` (repo privado aichef-blog): 408+27+18 posts con HTML íntegro + 7 pages + 1.523 medios (831 MB) 0 fallos; **cola programada de WP congelada** (27 future→draft) → WP clavado en 408 = censo canónico; **Abacus con luz verde para apagarse**; ⚠️ **WP/Hostinger NO se apaga hasta el cutover 8B.5**. **Port ES**: rama `fase8b-blog` — infra blog (content collection Astro 5 + BlogPost layout con BlogPosting/BreadcrumbList/FAQPage + hub /blog paginado 24/pág + 6 categorías + RSS sin deps + @tailwindcss/typography) + conversor `fase8b-wp2md.py` (excerpt→meta description ≤158c, categoría por cluster de la auditoría 06-15, enlaces internos→/blog/{slug}, Tier D→hub, **gotchas cazados por el gate: CDN Jetpack i0.wp.com (la MAYORÍA de las imágenes), data-permalink de galerías, variantes -WxH, tag/root/catch-all, ficheros con nombre percent-encoded**) + clasificador `fase8b-tiers.py` (**A134/B52/C160/D62**; D = 50 pSEO→NO migran + 10 escuelas US [candidatas a reciclarse en escuelasdecocina.com, decisión John] + 2 duplicados) + optimizador `fase8b-optimize-assets.py` con guard térmico (**723→394 MB**: 8 WAV→AAC 191→14 MB, 124 PNG sin alfa→JPG, JPG q75/1200w; ⚠️ lección: re-comprimir JPGs ya eficientes con sips los ENGORDA — se restauró el mínimo por fichero desde el export) + mapa 301 `fase8b-301map.py` (143 reglas: excepciones Tier D + genérica /:slug + /wp-content/uploads/*→/blog-assets/:splat; ACTIVAR SOLO en cutover) + **gate `fase8b-gate.py` 5.627 checks 0 fallos** (local: 346 .md frontmatter/categorías/0 refs residuales/assets existen; remoto en branch deploy: hub+paginación 15 págs+20 posts muestra title/canonical/BlogPosting/hreflang+6 categorías+RSS 30 items+10 assets+anti-regresión F1/F4/F2/EN). Pipeline idempotente: convertir→optimizar. **MERGE A PROD EJECUTADO el mismo día (`1b2336e`, luz verde de John): aichef.pro/blog LIVE con 346 posts.** Antes del merge, por indicación de John ("muchas recetas están en tutoriales, error arrastrado del WP"): **recategorización con jueces adversariales** (Workflow 2 Sonnet: refutador de falsos positivos del regex 'receta' + clasificador del resto) → categoría **Recetas** nueva (53 posts: 45 con 'receta' en slug + 8 platos sin ella tipo ceviche/coulant/pulpo), `recetario-pro-ai` fusionada, **3 posts de glosario rescatados de tutoriales** (goma-xantana/llm/token), tutoriales queda en 11 reales; todo materializado en `fase8b-cat-overrides.json` (67 overrides deterministas). + **lastmod real por post** en el sitemap (`blog-lastmod.json` generado por el conversor) + expectativas DINÁMICAS de sitemap en fase6-gate/vigilancia (696+367=1.063 computado del repo). **Verificación PROD post-merge TODA VERDE: gate 8B 5.627 + gate F6 post-cutover 2.283 + gate F5 767 + vigilancia** (⚠️ el F6 post-cutover se invoca `fase6-gate.py https://aichef.pro` — sin arg corre modo staging obsoleto). Housekeeping: sitemap-index reenviado a GSC, staging allowed_branches→main, rama borrada. Decisiones John: librerías→8C ✅ · escuelas US NO se reciclan (criterio nuevo: "lo que no monetiza no se mantiene"; escuelasdecocina.com candidato a cierre). **Próximo: (1) slices refresh Tier A (134) con REGLA CAPITAL; (2) cutover subdominio 8B.5 (DNS blog.aichef.pro→alias del site prod + activar fase8b-redirects-blog.txt) y SOLO ENTONCES apagar WP/Hostinger; (3) 8B.6+ idiomas (EN primero); (4) 8C páginas de agentes (absorbe las 34 librerías con 301).**

- **2026-07-19 (Fase 7 — 🚀 CUTOVER EJECUTADO: aichef.pro sirve ASTRO)** — Misma jornada que Fases 5 y 6, con luz verde explícita de John. **Arquitectura del swap (decisión que F5 dejó pendiente): el site de producción conserva TODO** (dominio+SSL, las 4 functions de dinero, env vars, webhooks) **y cambia lo que construye**: el `netlify.toml` raíz pasa de SPA a `publish astro-site/dist` + `cd astro-site && npm install && npm run build`; las functions quedan **same-origin** (adiós puente) y el **rollback es un revert o restaurar el deploy SPA anterior en Netlify (instantáneo)**. Red de seguridad: **branch deploy `cutover-astro` en el MISMO site de prod** validado ANTES del flip — build verde con sus env vars reales + `verify-purchase` same-origin 400 unknown_product + gates F5 767/767 y F6 2.278/2.278 + payment links + lang-redirect. Flip = merge `afc0758` → deploy atómico sin downtime. **Verificación POST-flip en prod: gate F5 767/767 + gate F6 (modo post-cutover: sitemap 696 vs snapshot, cabecera index,follow) 2.282/2.282 + spot-checks** (robots.txt con Disallows zona app, /sitemap.xml→301, llms.txt, pSEO Madrid, Stripe links, FAQPage, og cards, y 404 REAL — la SPA los enmascaraba con 200). Cambios del flip: meta robots index,follow en BaseLayout (staging sigue noindex por su cabecera, más restrictiva) · og-meta MUERTA (sin declarar; meta nativo) · lang-redirect declarada en el toml raíz · sin fallback SPA. **Desviación documentada del plan**: FunctionsOriginPatch y el bloque noindex de `astro-site/netlify.toml` NO se quitan — ambos son de staging (el patch es no-op fuera del hostname de staging) y mantienen el site de staging funcional como pre-prod permanente; las instrucciones de "quitar en cutover" asumían el modelo swap-de-site que se descartó. **GSC**: retirados sitemaps www duplicado y tienda stale (§5); alta de `sitemap-index.xml` + resubmit de `sitemap.xml` (301). La SPA queda en el repo como fuente de los islands (D5) — ya no se construye. **VIGILANCIA (hasta ~2026-08-16)**: GSC diario/frecuente — cobertura, 404/soft-404 nuevos, canibalizaciones; Stripe: vigilar compras post-pago con acceso normal. Rollback documentado en el propio toml.

- **2026-07-19 (Fase 6 — SEO nativo COMPLETA, gate 2.278 checks verde ×2)** — **El censo inicial destapó el hallazgo clave de la fase: 126 URLs del sitemap de prod NO estaban en Astro** (8 free tools + 8 landings SEO + hub `/herramientas-gratuitas` ×7 idiomas — los mejores performers no-marca — **nunca asignadas a ninguna fase del plan**) + los 5 legales del footer en 404 en staging desde Fase 1 + `/services` ×7 = soft-404 también en la SPA (excluido documentado). **S0.5**: 22 componentes SPA → **155 URLs como islands `client:only`** (patrón Fase 5): head-modules SEO server-side por componente (workflow **22 escritores + 22 verificadores adversariales Opus**, claves i18n verbatim; el verificador cazó el riesgo og:image que el generador ya cubría con el default `/og-image.jpg` de SEOHead), shim router +`useParams`+`Link`, 17 deps npm. **2 gotchas nuevos que valen dinero**: (1) los paquetes bare importados por ficheros cross-root NO resuelven en Netlify (base=astro-site, node_modules raíz inexistente) → **alias por paquete a `astro-site/node_modules`** (mismo gotcha que lucide-react en F5, generalizado; el build cayó con `Rollup failed to resolve import "i18next"`); (2) en el pipeline de Astro los imports de imágenes devuelven `ImageMetadata` (objeto), no URL string → los islands renderizaban `src="[object Object]"` (logo/avatares rotos) → **plugin Vite `cross-root-assets-as-url`** (`?url` solo para imágenes del src raíz). **S1 sitemap**: 696 URLs = 658 de prod − `/services`×7 + **45 de productos digitales** (0/44 estaban — dinero indexable nuevo); lastmod por URL portado de prod (658, snapshot re-ejecutable `fase6-lastmod-map.py`); exclusiones de paridad (legales/alias/zona app); `/sitemap.xml`→301→`sitemap-index.xml` (GSC/robots.txt intactos); alternates xhtml NO (hreflang nativo en el HTML de TODAS las páginas = cobertura superior a los 521/658 del sitemap de prod). **S2**: llms.txt + llms-full.txt byte-idénticos. **S3**: `lang-redirect` portado byte-idéntico y ACTIVO en staging (decisión: SE MANTIENE tras cutover; gate verifica 302 humano/200 bot/nolang/cookie) + www→apex + security headers + caché `/_astro/*`. **S4**: los 3 JSON-LD globales de SEOHead (Organization/WebSite/SoftwareApplication con ratingCount determinista en build) en BaseLayout — cierra la deuda anotada en F5. **S5**: **51 OG cards ES** de use cases (estilo tarjeta idéntico al set EN, Nano Banana 2, ~$3.90) con QA visual de 4 revisores Sonnet — 3 defectuosas cazadas y regeneradas (subtítulo duplicado, palabra omitida, texto embebido); réplica byte-idéntica en `public/` de la SPA → **prod ya sirve las 51 og:image que referenciaba en 404**. **QA runtime en Chrome**: calculadora interactiva funcional, assets OK tras fix, `/fr` con paridad EXACTA de comportamiento de idioma (cuerpo ES por localStorage del detector, IGUAL que prod; el title server-side FR de Astro MEJORA a la SPA que lo sirve ES) y overflow horizontal inexistente (medido `bodyScrollWidth==innerWidth` en ambos entornos tras aviso de John). Gates persistidos: `fase6-registry-marketing.py` (triple cross-check que aborta) + `fase6-generate-marketing.py` (generador determinista 177 ficheros) + `fase6-gate.py` (**2.278 checks**: 155 URLs × título/desc/canonical/hreflang×8/island/chunk/schemas + diff sitemap EXACTO contra prod LIVE + llms + lang-redirect + rich results + anti-regresión F2/F4/F5). Commits: `43a5e0d` + `331f936` + `c3ebdd8` + `2719e9e`. Pendientes menores nuevos: llms.txt enlaza `/precios`/`/sobre-nosotros`/`/contacto`/`/faq` que NO existen (heredado de prod, candidato F8); cuerpo fr/de/it/pt/nl de las 16 landings/tools sale del fallback ES cuando el detector fija otro idioma (= SPA; traducciones existen parcialmente en los JSON — contenido es F8). **Próximo: Fase 7 CUTOVER — PARAR Y ESPERAR OK DE JOHN** (quitar FunctionsOriginPatch + bloque noindex del toml + meta robots index en BaseLayout + swap del site + vigilancia GSC + housekeeping sitemaps GSC).

- **2026-07-03** — Plan aprobado. Fase 0 arrancada: scaffold `astro-site/` creado a mano (sin npm install local, D8) en commit `8bd1d40`.
- **2026-07-03 (Fase 1, slice 1)** — Home ES esqueleto LIVE en staging (commit `1564560`, port por subagente Opus): Header + Hero + Pricing + FAQ + Footer con paridad Tailwind, i18n leyendo los JSON de la SPA (fuente única, import cross-root verificado en build de nube), schemas Product+FAQPage, Radix→nativo (details/summary, hover CSS). Gates verificados por curl: hreflang×8, "3.000 créditos mensuales", "Max Anual", 2 JSON-LD, **13.470 bytes de texto server-side (vs 2.726 de TODA la SPA en producción)**. Pendiente slice 2: secciones restantes de la home (TrustedBy, Screenshot, Categories, Finder, Showcases, WorldCookbooks, About, Features, Gallery, FeaturedApps, ChefSection, CategoryCTAs) + luego ×7 idiomas. Regla nueva de John para Fases 2-3: gate anti-páginas-huérfanas + interenlazado.
- **2026-07-03 (cierre) — Slice 1 APROBADO por John** tras 3 fixes verificados en staging (`7c2be6f`): (1) mega-menús abrían todos a la vez — clase `group` en el `<ul>` ancestro, quitada; (2) palabra rotatoria del hero invisible al volver a la pestaña — throttling de timers en background, arreglado con guard `document.hidden` + `visibilitychange` (bug latente que TAMBIÉN existe en la SPA prod); (3) favicon de Lovable — `favicon.ico` era el corazón de Lovable aunque `favicon.png` ya era el icono bueno; reemplazado el .ico en staging Y producción. **DIRECTIVA DE JOHN: a partir de ahora ejecución AUTÓNOMA, slice a slice, sin revisión suya por cambio — la SPA original es la spec, replicar con paridad y verificar gates por mi cuenta.** Próximo objetivo: slice 2 de la home (TrustedByLogos, ScreenshotSection, SocialProofStrip, AppsCategories, AppsFinder, CreatividadShowcase, WorldCookbooks, BusinessToolsShowcase, CityResourcesStrip, ModernAbout, ModernFeatures, AIImageGallery, FeaturedApps, ModernChefSection, CategoryCTAs, ConversionNotifications, WhatsAppFloatingButton, AIToolsBanner — comparar contra `src/pages/Index.tsx`), después ×7 idiomas + /precios.
- **2026-07-03 (cont.)** — Staging creado VÍA CLI/API de Netlify (sin pasos manuales): site **`aichef-astro-staging`** (id `dc777725-7e95-4336-876e-a5a9b568fe75`), URL **https://aichef-astro-staging.netlify.app**, repo conectado base `astro-site` / branch `main` / publish `astro-site/dist`. Primer build disparado por API. Gotcha CLI aprendido: `netlify api createSite/updateSite` requieren los campos dentro de `"body": {…}`. Pendiente al retomar: confirmar gate Fase 0 (deploy verde + curl con title/hreflang×8 + header X-Robots-Tag noindex) si no quedó verificado hoy.
- **2026-07-11 (Fase 1, slice 2 — CERRADO, gates verdes)** — Home ES COMPLETA en staging (commit `b9bd45f`, 97 files / +2.283): las 18 secciones restantes de `Index.tsx` portadas (TrustedByLogos → WhatsAppFloatingButton, orden exacto de la SPA; **ModernAbout = import muerto en la SPA, no se porta**). Ejecución: workflow de 15 agentes Opus EN SECUENCIA ESTRICTA (regla térmica; CPU 47-59 °C todo el rato, pico 64 °C pre-arranque esperando a enfriar) — 5 batches × (port + revisor adversarial + fixer). Issues reales cazados por los revisores: conflictos twMerge de shadcn inlineados sin colapsar (colores Badge/Button default, `p-6`+`p-0`, `text-2xl`+`text-lg`), swipe táctil del carrusel FeaturedApps, assets sin reportar. Infra nueva: `lucide-react` en deps de astro-site + **alias Vite** `lucide-react` → node_modules local (el cross-root `src/data/apps.ts` lo importa y el node_modules raíz NO existe en el build de Netlify base=astro-site) + `tObjects()` en translations.ts + 75 assets copiados a `astro-site/public/`. Gotcha ScreenshotGallery: NO usa embla/zoom (la descripción previa era errónea); es swap de imagen con fade + auto-rotate 4s + play/pause — portado fiel. **Gates verificados por curl (UA Googlebot): HTTP 200 + noindex · hreflang×8 · title OK · 2 JSON-LD (Product+FAQPage) · 16/16 secciones server-side con textos reales de es.json · assets muestra 4/4 en 200 · 23.235 bytes de texto visible server-side (+72% vs 13.470 del slice 1; TODA la SPA en prod: 2.726)**. Nota paridad: `gradient-text`/`hover-card`/`animate-fade-in(-up)` se usan en clases pero NO están definidas ni en la SPA ni en Astro (no-op en ambos → paridad; candidata a definirse en Fase 8, no antes por D6). Próximo slice: **home ×7 idiomas** (index en `en/fr/de/it/pt/nl` reutilizando los componentes con prop `lang`) + `/precios`.
- **2026-07-18 (Fase 1, slice 3 — CERRADO, gates verdes ×7)** — Home en los 7 idiomas LIVE en staging (commits `272db46` + `3abfbca`): 6 páginas `astro-site/src/pages/{en,fr,de,it,pt,nl}/index.astro` reutilizando los 22 componentes con prop `lang`, title/meta desde `pages.index.seo_*` de cada JSON (mismas claves que la SPA). Ejecución: workflow 8 agentes en secuencia estricta (1 sonnet creación + 6 lotes Opus de auditoría adversarial i18n + 1 fixer Opus; CPU 42-50 °C todo el rato). **Auditoría i18n de los 23 componentes vs SPA: solo 2 hallazgos** — (1) MEDIA, aplicada: el menú móvil usaba las etiquetas largas del desktop; la SPA tiene etiquetas cortas propias en móvil (ModernHeader.tsx 581-585/599-604) → arrays `roleItemsMobile`/`conceptItemsMobile` verbatim, 77 labels verificadas byte a byte, slugs intactos; (2) BAJA, anotada sin aplicar: aria-label "Abrir/Cerrar menú" en ES fijo para todos los idiomas (la SPA no tiene aria-label; candidata a claves i18n en Fase 8). **GOTCHA NUEVO — `build: { format: 'file' }` en astro.config.mjs**: con el default `directory`, `/en` respondía **301 → `/en/`** (rompe paridad D6: la SPA y el sitemap usan URLs sin barra); con `file` se genera `en.html` y `/en` responde 200 (`/en/` normaliza 301 → `/en`). Aplica a TODAS las páginas anidadas futuras. **Gates verificados (curl UA Googlebot)**: 200+noindex ×7 · `<html lang>` correcto ×7 · title y meta description == `seo_*` del JSON byte-exactos (en/fr/de/it/pt/nl) · canonical `/{lang}` · hreflang×8 · 2 JSON-LD localizados (faq.q1 nativo dentro del FAQPage verificado en en/de/nl) · 5 textos nativos de secciones distintas server-side ×7 · bytes visibles: es 23.032 / en 19.376 / fr 24.073 / de 21.188 / it 20.781 / pt 21.216 / nl 20.515. **HALLAZGO DE ALCANCE: `/precios`, `/sobre` y `/servicios` NO existen en la SPA** (0 rutas en App.tsx, 0 en sitemap; "Precios" del header = ancla `/#pricing`) — la Fase 1 restante real es `/mentoria-online` (×idiomas vía `/:lang/mentoria-online`) y `/formacion-presencial` (solo ES + `/es/formacion-presencial`); crear `/precios` sería URL/copy nuevo → decisión de producto para Fase 8. Anomalía menor anotada: `es.json pages.index.seo_description` dice "marketing gastronomico" SIN tilde (erosión previa; el hardcode ES de index.astro la lleva) → resolver en la auditoría de acentos pendiente. Próximo slice: **/mentoria-online ×7** + `/formacion-presencial`.
- **2026-07-18 (Fase 1, slice 4 — CERRADO, gates verdes ×7 · FASE 1 COMPLETA)** — `/mentoria-online` ×7 idiomas + `/formacion-presencial` LIVE en staging (commit `f8ab209`, 17 files / +1.466): `MentoriaOnlinePage.astro` (10 secciones en orden SPA, 2 JSON-LD, acordeón nativo, todo vía `t()`) + 7 wrappers con prop `lang`; `formacion-presencial.astro` solo ES usando la prop nueva `locales` de BaseLayout (hreflang limitado) + redirect 301 `/es/formacion-presencial` → `/formacion-presencial`. Ejecución: workflow 5 agentes Opus en secuencia (CPU 46-59 °C); hallazgo único del revisor adversarial (gap de paridad SEO en los 7 wrappers de mentoría) aplicado y verificado por lectura en los 7; hard-check extra: `locales` sin la prop cae a los 7 idiomas → páginas existentes intactas. **INCIDENTE: apagón térmico del Mac a las ~13:18** justo al lanzar los gates — sesión reconstruida desde el transcript JSONL (los gates se relanzaron y el script del workflow Fase 2 se recuperó del propio transcript; `/private/tmp` se vació con el reinicio). **Gates verificados post-recovery (curl UA Googlebot)**: 200 ×7 mentoría + 200 formación · titles localizados ×7 · canonical por idioma OK · hreflang×8 en mentoría / solo `es`+`x-default` en formación · alias 301 OK · assets `formacion-workshop.jpg` + `chef-john-guerrero.png` en 200 · home sin regresión (title + hreflang OK tras el cambio de BaseLayout). Próximo: **Fase 2 use cases — 441 URLs ×7 idiomas** (lib rutas cross-root + template `UseCasePageContent` + 2 hubs + 14 páginas dinámicas + assets; workflow `wf_d5f5e08e` relanzado con porters/revisores/fixers Opus + assets Sonnet).
- **2026-07-18 (Fase 2 — use cases COMPLETA, gates verdes 441/441)** — Cluster de use cases entero LIVE en staging (commit `515fe09`, 451 files / +2.554): `lib/use-cases.ts` (SEGMENTS/CONSULTOR_HUB verbatim de UseCasePage.tsx; `spokeStaticPaths` = 62 paths por idioma; `useCasePath`/`alternatesFor`/alternates de hubs) + `UseCasePageContent.astro` (port 1:1 de UseCasePage.tsx, 835 líneas) + `UseCasesHubPage.astro` + `ConsultoriaGastroProHubPage.astro` + 7 wrappers hub + 7 catch-alls `[...rest].astro` (páginas dinámicas vía getStaticPaths, NO 441 ficheros) + `Icon.astro` (50 SVG lucide inline) + 430 assets verificados byte a byte con `diff -rq` (366 gallery + 63 og/use-cases + og-image.jpg). Fallback de contenido `content[lang] || content.es` y `slug[lang] || slug.es` idéntico a la SPA. **INCIDENTE: 2º apagón térmico del día (~14:21)** — mató el workflow tras escribir los hubs (sin revisar aún); recuperación completa desde `~/.claude/projects/<proj>/<sesión>/subagents/workflows/<wf>/journal.jsonl` + `workflows/wf_*.json` (el script completo viaja dentro; `/private/tmp` se vació otra vez con el reinicio) → método persistido en memoria (`reference_recuperacion-apagon-termico-workflows.md`). Ejecución: workflow `wf_d5f5e08e` (lib → template → revisor adversarial — cazó import estático build-blocking de componente inexistente — → fixer → porter hubs †apagón; todo Opus) + continuación `wf_ba9e3d83` (revisor hubs Opus: **0 hallazgos críticos/altos/medios**, bloques UI byte-idénticos, 62 params únicos ×7 sin colisiones; assets Sonnet). **Gates (curl UA Googlebot, staging): 441/441 en 200 · title + meta description + canonical exacto + H1 + hreflang×8 en las 441 · FAQPage en 434/434 spokes+consultor** (los 7 hubs principales sin FAQPage = paridad: `UseCasesHub.tsx` tiene 0 refs FAQ) · muestra profunda por idioma OK. Hallazgos BAJA anotados (no bloquean): (1) `finalBody` DE del hub consultor dice "5 Anwendungen pro Monat" EN LA SPA (resto pre-migración a créditos 2026-06-12) → fix en `ConsultoriaGastroProHub.tsx:202` + re-mirror al .astro, NUNCA solo en Astro; (2) 51 ogImages ES de role/concept/task referencian ficheros que tampoco existen en la SPA (solo hay EN + consultor) → candidatos a `generate-images` en Fase 6/8. Nota paridad: titles/contenido de spokes role/concept/task en FR/DE/IT/PT/NL salen en ES por el fallback `content.es` (= comportamiento SPA; traducir contenidos sería Fase 8, no paridad). Próximo: **Fase 3 — pSEO 75 ciudades** (1 template data-driven, est. 1 sesión).
- **2026-07-18 (Fase 3 — pSEO ciudades COMPLETA, gates verdes 76/76)** — Cluster pSEO entero LIVE en staging (commit `ce53e18`): `lib/pseo-cities.ts` (`cityStaticPaths`: **15 ciudades × 5 modifiers = 75 paths** + hub = 76 URLs, SOLO ES) + `PSeoCityPageContent.astro` (port 1:1 de `PSeoCityPage.tsx`, 478 líneas: 12 secciones en orden SPA, data moat por ciudad —costes/regulación/salarios/barrios/ticket—, 3 JSON-LD BreadcrumbList/Service/FAQPage) + `PSeoCitiesHubPage.astro` (BreadcrumbList + CollectionPage, 75 enlaces) + 6 wrappers (hub + 5 `[ciudad].astro`) + `Icon.astro` +MapPin/ScrollText. **Corrección de memoria: el cluster son 15 ciudades × 5 modifiers, no "75 ciudades"** (eran 75 páginas). Ejecución: workflow `wf_34d1c4bb`, 7 agentes secuenciales ~34 min (porters/revisores/fixers Opus + assets Sonnet; CPU 47-58 °C). Hallazgo MEDIA del revisor APLICADO: los 5 `og-pseo-*.jpg` no estaban en `astro-site/public/` → OG en 404 para las 75 URLs post-cutover; copiados con md5 verificado. Hallazgos BAJA anotados sin acción: (1) **`/saas-trial`** (CTA primario del modifier software) es ruta muerta YA en la SPA — bug de DATOS preexistente, post-cutover será 404 duro; decidir en datos compartidos (candidato: `/herramientas-ia-para-restaurantes`); (2) hub: la SPA emite 0 hreflang (`disableAutoHreflang`) y Astro emite `es`+`x-default` = mejora Fase 0 sancionada; (3) JSON-LD globales de SEOHead (Organization/WebSite/SoftwareApplication con rating en vivo) omitidos a propósito en BaseLayout → pendiente slice de plataforma que los reintroduzca site-wide; (4) `meta robots index,follow` → añadir en BaseLayout en el cutover (Fase 7) al retirar `X-Robots-Tag`. **Gates (curl UA Googlebot, staging): 76/76 en 200 · title + meta description + canonical exacto + H1=1 en las 76 · FAQPage+Service en 75/75 ciudad · CollectionPage en hub · BreadcrumbList 76/76 · hreflang=2 (es+x-default) ×76 · anti-regresión OK** (home, spokes Fase 2 ES/EN y mentoría en 200 con titles correctos tras tocar Icon.astro). Próximo: **Fase 4 — productos digitales (33 landings consolidadas)** — ⚠️ **PARAR Y AVISAR A JOHN ANTES DE EMPEZAR** (toca dinero/Stripe; checklist de los 2 bugs históricos de access gates y paths de descarga).
- **2026-07-18 (pre-Fase 4 — HOTFIX PRODUCCIÓN `7ab25b7`, smoke verde)** — El censo previo a Fase 4 destapó que **17 de los 44 productos entregaban el eBook de €9 post-pago** (mapas de las 4 netlify functions desincronizados desde mayo; fallback silencioso de `verify-purchase.ts` a `pro-prompts-ebook`; `resend-access` solo conocía 4/44). Fix desplegado y verificado con smoke en producción: mapas 44/44/44/43+especial (doctrina `MISSING: 0`, 483 paths), producto desconocido → 400 `unknown_product` (fallback legacy conservado), diff +614/−0, revisor adversarial 0 hallazgos. Las functions son backend COMPARTIDO SPA/Astro (D4/D5: la migración no las toca) → el fix vale para ambos. Doctrina actualizada con **Bug #5**. CORRECCIÓN AL PLAN: son **44 productos** (no 33) — 44 landings + 44 access-gates + 44 dashboards + hub + admin = 134 rutas; 0/44 landings están en el sitemap actual (gap del generador → corregir en Fase 6, +45 URLs de dinero indexables). Pendiente de John: compra test (kit-tareas-asador + refund) o magic link admin; y revisar en Stripe compras reales de los 17 desde mayo (afectados → regenerar acceso + email). **[RESUELTO mismo día: John confirmó CERO compras de los 17 → incidente cerrado sin afectados.]**
- **2026-07-19 (Fase 5 — zona app post-pago CÓDIGO COMPLETO, gate 767 checks verdes; happy-path E2E pendiente de John)** — Las **89 rutas** de la zona app (44 access + 44 library + `/admin/generar-acceso`) LIVE en staging como **islands React `client:only` que importan los componentes de la SPA cross-root TAL CUAL** (D5, cero copias; commits `ed8e85e` infra + `cad7717` 88 páginas + `79d6661` admin). Infra: shims `react-router-dom` (useSearchParams/useNavigate/Navigate/useLocation estables sobre window.location — referencias estables OBLIGATORIAS: params/navigate van en deps del useEffect de verificación) y `react-helmet-async` no-op vía alias Vite; alias `@` → src raíz (astro-site no usa `@/` propio); tailwind content acotado al cierre (censo 107 ficheros, único dep npm extra: ninguno); BaseLayout con prop `noindex` (meta robots server-side + omite canonical/hreflang — MEJORA vs SPA donde dependía de JS) y description opcional; filtro sitemap `-access/-library//admin`; robots.txt con Disallow ×5 grupos UA. **GOTCHA Netlify: ignora redirects sobre `/.netlify/*`** (gate S0 404) → puente staging→functions de prod con `FunctionsOriginPatch.astro` (patch de window.fetch keyed por hostname staging; CORS `*`+OPTIONS verificados en las 4 functions; no-op en prod post-cutover, QUITAR en Fase 7). **Generación determinista SIN LLM**: `scripts/astro-migration/fase5-generate-zona-app.py` (132 ficheros; props ProtectedRoute y titles extraídos VERBATIM de App.tsx/Helmet; cross-checks que abortan) + gate `fase5-gate-s1-s2.py` (**767 checks, 0 fallos**: 88 URLs × title/noindex/island/patch/chunk + admin + robots + sitemap + anti-regresión; gotchas del gate: Python de macOS sin CAs → fetch vía curl; comparar títulos DES-escapados F&amp;B==F&B; hub F2 es `/usos`). Revisión adversarial Opus del S0: APTO CON FIXES (C1 proxy = el gotcha Netlify, corregido; resto refutado sin hallazgos). **Fix operativo en la SPA (prod, deploy verde)**: select de `/admin/generar-acceso` tenía 27/44 productos → 44/44 (nombres/precios verbatim del hub; cuadre admin==registro==verify-purchase 44/44). **E2E navegador (Chrome, sin Playwright) del camino negativo VERDE**: gate hidrata y procesa `?jwt=` inválido → fetch reescrito a `https://aichef.pro/.netlify/functions/verify-purchase` (finalUrl capturada, 403 JSON) → UI de error con props correctas; `/kit-tareas-asador-library` sin token → ProtectedRoute+Navigate shim → redirect a la landing. **GATE DE ACEPTACIÓN CERRADO mismo día (happy-path E2E con magic link real de John)**: John generó el magic link DESDE la página admin de Astro en staging (valida S3 end-to-end: password → función de prod vía patch → link + email a local@gastroseo.com) y lo validó en prod; Claude lo ejecutó después contra STAGING con el caso más quirky (gate vanilla Pro Prompts, sin campo `product` → fallback legacy): verify → `pro-prompts-jwt` en localStorage → redirect shim a `/pro-prompts-library` → library compuesta (11 subcomponentes) renderizada → get-download-urls vía patch → 3 URLs tokenizadas → **3/3 binarios reales (PDF 853KB + DOCX + XLSX, content-type correcto, cero HTML del SPA-fallback = Bug #2 limpio)**. Fase 5 COMPLETA en 1 sesión (est. 2-3). ⚠️ Cutover (Fase 7): quitar FunctionsOriginPatch + bloque noindex del toml. Registro nuevo: `astro-site/src/lib/zona-app.ts` (44 tuplas; NO es config de dinero — las props viven en los wrappers de la SPA). Quirks preservados: URL `-completo-` hotel, typo `Hamburguseria`, pro-prompts vanilla sin `product` (fallback legacy).
- **2026-07-18/19 (Fase 4 — productos digitales COMPLETA, gate de aceptación 44/44 + hub)** — Las **44 landings + hub /productos-digitales** LIVE en staging en 4 slices (commits `ff9a96c` tareas ×19 · `ef8b213` kits ×5 + guías ×8 · `77743d4` planes ×10 · `9a24af7` eBook + Mega Pack + hub). **Consolidación conseguida: 465 componentes React clónicos → 4 templates .astro** (KitTareasLandingPage, KitExcelLandingPage, GuiaLandingPage, PlanNegocioLandingPage) **+ 42 ficheros de datos verbatim + 2 one-offs + hub** (44 tarjetas orden de negocio, filtros vanilla sobre DOM completo). Prerequisito resuelto: **44 env vars `VITE_STRIPE_PAYMENT_LINK_*` replicadas de producción al site de staging** (API Netlify, scope builds) — sin ellas los CTAs caían al fallback `#comprar`. Hallazgos de arquitectura: las 4 líneas son DOM-uniformes (18 secciones orden idéntico incluso ENTRE líneas); divergencias DOM reales cazadas y cubiertas con props: 1 tareas (heading Hotel), 5 kits, 2 guías (CompatibleAppsMarquee solo dark-kitchen, layout BonusSection), 1 planes (H2 CtaFinal A/B) — la distinción "Línea A/B" de planes resultó ser SOLO de contenido → 1 template. Fixes de revisores aplicados en 13 productos (OG images faltantes, fallback galería, schema image eBook, asset hero hub). **Gate de aceptación (criterio del plan): 44/44 checkouts con el href del CTA byte-idéntico al payment link real de Stripe** (comparado contra las env vars exportadas de producción; 4-5 CTAs por landing) **+ QA de enlaces hub 44/44 + 3 JSON-LD (Product/FAQPage/BreadcrumbList) ×42 landings de línea + titles/canonical/H1 ×44**. Ejecución: 4 workflows (99 agentes totales, arquitectos/revisores/fixers Opus + extractores Sonnet, cadenas ×3; CPU 43-50 °C toda la noche). Próximo: **Fase 5 — zona app (44 access-gates + 44 dashboards como islands, funciones intactas, admin, compra test real)** — ⚠️ **ESPERAR OK DE JOHN** (dinero). Recordatorio Fase 5: quirks `/pro-prompts-library-access` y `/kit-tareas-hotel-completo-access`; noindex server-side para `-access`/`-library` + Disallow robots.txt de respaldo.
