---
// Fase 5 — dashboard post-pago de Guía Restaurante Mexicano (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de GuiaRestauranteMexicanoDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import GuiaRestauranteMexicanoLibraryIsland from '../islands/library/GuiaRestauranteMexicanoLibraryIsland';
---

<BaseLayout title="Guía Restaurante Mexicano — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <GuiaRestauranteMexicanoLibraryIsland client:only="react" />
</BaseLayout>
