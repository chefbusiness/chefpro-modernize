---
// Fase 5 — dashboard post-pago de Mega Pack Tareas (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de MegaPackTareasDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import MegaPackTareasLibraryIsland from '../islands/library/MegaPackTareasLibraryIsland';
---

<BaseLayout title="Mega Pack Tareas Recurrentes — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <MegaPackTareasLibraryIsland client:only="react" />
</BaseLayout>
