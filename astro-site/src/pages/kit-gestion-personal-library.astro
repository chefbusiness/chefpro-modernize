---
// Fase 5 — dashboard post-pago de Kit de Gestión de Personal (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de KitGestionPersonalDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import KitGestionPersonalLibraryIsland from '../islands/library/KitGestionPersonalLibraryIsland';
---

<BaseLayout title="Kit Gestion de Personal y Turnos — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <KitGestionPersonalLibraryIsland client:only="react" />
</BaseLayout>
