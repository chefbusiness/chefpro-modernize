---
// Fase 5 — dashboard post-pago de Kit de Tareas Dark Kitchen (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de KitTareasDarkKitchenDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import KitTareasDarkKitchenLibraryIsland from '../islands/library/KitTareasDarkKitchenLibraryIsland';
---

<BaseLayout title="Kit de Tareas Dark Kitchen — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <KitTareasDarkKitchenLibraryIsland client:only="react" />
</BaseLayout>
