---
// Fase 5 — dashboard post-pago de Kit de Inventario (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de KitInventarioDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import KitInventarioLibraryIsland from '../islands/library/KitInventarioLibraryIsland';
---

<BaseLayout title="Kit Control de Inventario y Compras — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <KitInventarioLibraryIsland client:only="react" />
</BaseLayout>
