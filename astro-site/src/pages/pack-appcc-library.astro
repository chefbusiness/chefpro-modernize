---
// Fase 5 — dashboard post-pago de Pack APPCC (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de PackAppccDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import PackAppccLibraryIsland from '../islands/library/PackAppccLibraryIsland';
---

<BaseLayout title="Pack de Plantillas APPCC — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <PackAppccLibraryIsland client:only="react" />
</BaseLayout>
