---
// Fase 5 — dashboard post-pago de Guía Restaurante Gastronómico (GENERADO por scripts/astro-migration/
// fase5-generate-zona-app.py — editar el generador, no este fichero).
// Island client:only="react": ProtectedRoute lee localStorage en render
// síncrono (ReferenceError bajo SSR) → sin pase SSR, igual que el shell SPA.
// Title verbatim del <title> del Helmet de GuiaRestauranteGastronomicoDashboard.tsx (extraído por el generador).
import BaseLayout from '../layouts/BaseLayout.astro';
import FunctionsOriginPatch from '../components/FunctionsOriginPatch.astro';
import GuiaRestauranteGastronomicoLibraryIsland from '../islands/library/GuiaRestauranteGastronomicoLibraryIsland';
---

<BaseLayout title="Guía Restaurante Gastronómico — Dashboard | AI Chef Pro" noindex>
  <FunctionsOriginPatch />
  <GuiaRestauranteGastronomicoLibraryIsland client:only="react" />
</BaseLayout>
